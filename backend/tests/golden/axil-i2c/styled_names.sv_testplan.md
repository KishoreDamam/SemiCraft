# AXI4-Lite I2C Master — Test Plan

An open-drain AXI4-Lite I2C master. Software runs the bus one primitive at a time through CMD — optionally a START, then one byte written or read, then optionally a STOP — which is how a real transaction is assembled. The AXI frontend is the register-block generator spliced into this module.

_Generated by SemiCraft 0.4.0 — config hash `0023434d01dd`. Derived from `axil_i2c`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_i2c_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Quarter-period divisor: 2 aclk cycles (an SCL period is four times this; CLKDIV is writable at run time)
- Input synchroniser: 2 stages on both lines
- Registers: 5 (32 B decoded, 20 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

**Reset behavior:** The active-low sync reset returns the FSM to idle and releases both pull-downs, so the master comes up off a shared bus rather than holding it. The synchronisers reset high, which is the idle level of an open-drain line.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `p_sclOe`, `p_sdaOe`, `p_bvalid` | cycle 0: drive p_sclIn=1, p_sdaIn=1 | p_sclOe == 0; p_sdaOe == 0; p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 3: expected value of `p_bvalid`, `p_bresp` | cycle 3: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 4: expected value of `p_bvalid` | cycle 4: drive p_awaddr=4, p_awvalid=1, p_bready=1, p_wdata=19, p_wstrb=15, p_wvalid=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `p_bvalid`, `p_bresp` | cycle 6: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `p_bvalid`, `p_sclOe`, `p_sdaOe` | cycle 7: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 0; p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 9: expected value of `p_sclOe`, `p_sdaOe` | cycle 9: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 11: expected value of `p_sclOe`, `p_sdaOe` | cycle 11: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 13: expected value of `p_sclOe`, `p_sdaOe` | cycle 13: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 15: expected value of `p_sclOe`, `p_sdaOe` | cycle 15: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 17: expected value of `p_sclOe`, `p_sdaOe` | cycle 17: drive p_sclIn=0 | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 19: expected value of `p_sclOe`, `p_sdaOe` | cycle 19: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 25: expected value of `p_sclOe`, `p_sdaOe` | cycle 25: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 27: expected value of `p_sclOe`, `p_sdaOe` | cycle 27: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 29: expected value of `p_sclOe`, `p_sdaOe` | cycle 29: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 31: expected value of `p_sclOe`, `p_sdaOe` | cycle 31: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 33: expected value of `p_sclOe`, `p_sdaOe` | cycle 33: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 35: expected value of `p_sclOe`, `p_sdaOe` | cycle 35: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 37: expected value of `p_sclOe`, `p_sdaOe` | cycle 37: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 39: expected value of `p_sclOe`, `p_sdaOe` | cycle 39: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 41: expected value of `p_sclOe`, `p_sdaOe` | cycle 41: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-21 | Cycle 43: expected value of `p_sclOe`, `p_sdaOe` | cycle 43: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-22 | Cycle 45: expected value of `p_sclOe`, `p_sdaOe` | cycle 45: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-23 | Cycle 47: expected value of `p_sclOe`, `p_sdaOe` | cycle 47: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-24 | Cycle 49: expected value of `p_sclOe`, `p_sdaOe` | cycle 49: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-25 | Cycle 51: expected value of `p_sclOe`, `p_sdaOe` | cycle 51: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-26 | Cycle 53: expected value of `p_sclOe`, `p_sdaOe` | cycle 53: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-27 | Cycle 55: expected value of `p_sclOe`, `p_sdaOe` | cycle 55: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-28 | Cycle 57: expected value of `p_sclOe`, `p_sdaOe` | cycle 57: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-29 | Cycle 59: expected value of `p_sclOe`, `p_sdaOe` | cycle 59: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-30 | Cycle 61: expected value of `p_sclOe`, `p_sdaOe` | cycle 61: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-31 | Cycle 63: expected value of `p_sclOe`, `p_sdaOe` | cycle 63: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-32 | Cycle 65: expected value of `p_sclOe`, `p_sdaOe` | cycle 65: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-33 | Cycle 67: expected value of `p_sclOe`, `p_sdaOe` | cycle 67: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-34 | Cycle 69: expected value of `p_sclOe`, `p_sdaOe` | cycle 69: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-35 | Cycle 71: expected value of `p_sclOe`, `p_sdaOe` | cycle 71: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-36 | Cycle 73: expected value of `p_sclOe`, `p_sdaOe` | cycle 73: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-37 | Cycle 75: expected value of `p_sclOe`, `p_sdaOe` | cycle 75: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-38 | Cycle 77: expected value of `p_sclOe`, `p_sdaOe` | cycle 77: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-39 | Cycle 79: expected value of `p_sclOe`, `p_sdaOe` | cycle 79: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-40 | Cycle 81: expected value of `p_sclOe`, `p_sdaOe` | cycle 81: drive p_sdaIn=0 | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-41 | Cycle 83: expected value of `p_sclOe`, `p_sdaOe` | cycle 83: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-42 | Cycle 85: expected value of `p_sclOe`, `p_sdaOe` | cycle 85: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-43 | Cycle 87: expected value of `p_sclOe`, `p_sdaOe` | cycle 87: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-44 | Cycle 89: expected value of `p_sclOe`, `p_sdaOe` | cycle 89: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-45 | Cycle 91: expected value of `p_sclOe`, `p_sdaOe` | cycle 91: drive p_sdaIn=1 | p_sclOe == 1; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-46 | Cycle 93: expected value of `p_sclOe`, `p_sdaOe` | cycle 93: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-47 | Cycle 95: expected value of `p_sclOe`, `p_sdaOe` | cycle 95: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-48 | Cycle 97: expected value of `p_sclOe`, `p_sdaOe` | cycle 97: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-49 | Cycle 103: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 103: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 2; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-50 | Cycle 106: expected value of `p_bvalid`, `p_bresp` | cycle 106: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-51 | Cycle 107: expected value of `p_bvalid` | cycle 107: drive p_awaddr=4, p_awvalid=1, p_bready=1, p_wdata=21, p_wstrb=15, p_wvalid=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-52 | Cycle 109: expected value of `p_bvalid`, `p_bresp` | cycle 109: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-53 | Cycle 110: expected value of `p_bvalid` | cycle 110: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-54 | Cycle 182: expected value of `p_sclOe`, `p_sdaOe` | cycle 182: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-55 | Cycle 184: expected value of `p_sclOe`, `p_sdaOe` | cycle 184: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-56 | Cycle 186: expected value of `p_sclOe`, `p_sdaOe` | cycle 186: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 0; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-57 | Cycle 188: expected value of `p_sclOe`, `p_sdaOe` | cycle 188: no new stimulus this cycle (holds prior-driven values) | p_sclOe == 1; p_sdaOe == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-58 | Cycle 202: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 202: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 10; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-59 | Cycle 204: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 204: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 45; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `p_aclk` — free-running, `#5` half-period.
- **Reset:** `p_areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 205 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_aclk` | clock | free-running | n/a |
| `p_areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_awaddr` | input | yes (varies across the run) | no |
| `p_awvalid` | input | yes (varies across the run) | no |
| `p_awready` | output | n/a (output) | no |
| `p_wdata` | input | yes (varies across the run) | no |
| `p_wstrb` | input | yes (varies across the run) | no |
| `p_wvalid` | input | yes (varies across the run) | no |
| `p_wready` | output | n/a (output) | no |
| `p_bresp` | output | n/a (output) | yes |
| `p_bvalid` | output | n/a (output) | yes |
| `p_bready` | input | yes (varies across the run) | no |
| `p_araddr` | input | yes (varies across the run) | no |
| `p_arvalid` | input | yes (varies across the run) | no |
| `p_arready` | output | n/a (output) | no |
| `p_rdata` | output | n/a (output) | yes |
| `p_rresp` | output | n/a (output) | yes |
| `p_rvalid` | output | n/a (output) | yes |
| `p_rready` | input | yes (varies across the run) | no |

#### I2C bus

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_sclIn` | input | yes (varies across the run) | no |
| `p_sdaIn` | input | yes (varies across the run) | no |
| `p_sclOe` | output | n/a (output) | yes |
| `p_sdaOe` | output | n/a (output) | yes |

**Gaps:**

- `p_awready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `p_wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `p_arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `scl_released_after_reset` | `$rose(areset) |-> scl_oe == 1'd0` | `aclk` | none |
| `sda_released_after_reset` | `$rose(areset) |-> sda_oe == 1'd0` | `aclk` | none |
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- Single master: no bus arbitration and no lost-arbitration detection, so this must be the only master on the bus.
- No repeated START as a distinct primitive — issue a STOP and a fresh START, which works with most devices but is not identical on the wire.
- No 10-bit addressing and no general-call handling; the address is just the first byte software writes.
- No SMBus timeout, no PEC, no clock-low timeout — a slave that stretches forever stalls the master forever.
- No slave mode.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
