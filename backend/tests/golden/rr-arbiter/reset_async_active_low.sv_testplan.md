# Round-Robin Arbiter — Test Plan

An 4-way round-robin arbiter: it grants a shared resource to at most one requester per cycle (one-hot grant, asserted one cycle after the request), rotating priority so that under sustained contention every requester is served at least once every 4 cycles.

_Generated by SemiCraft 0.4.0 — config hash `698c887b1d68`. Derived from `rr_arbiter`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `rr_arbiter_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Requesters: 4 (req/grant are 4 bits, pointer is 2 bits)
- Grant style: registered (grant is a flop, one extra cycle of latency, cleaner boundary timing)
- Arbitration policy: rotate — priority advances past the granted requester every grant (pure round-robin)
- Reset: async, active-low
- Output language: SystemVerilog

**Reset behavior:** The active-low (asserted when 0) reset clears the priority pointer to 0 and the grant register to all-zero asynchronously on assertion of reset (independent of the clock), so after reset requester 0 holds highest priority and no grant is asserted.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 2: expected value of `grant_valid`, `grant` | cycle 2: drive req=1 | grant_valid == 1; grant == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 8: expected value of `grant_valid` | cycle 8: drive req=8 | grant_valid == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `clk` — free-running, `#5` half-period.
- **Reset:** `rst_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 10 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `clk` | clock | free-running | n/a |
| `rst_n` | reset | reset sequence only | n/a |

#### Arbitration

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `req` | input | yes (varies across the run) | no |
| `grant` | output | n/a (output) | yes |
| `grant_valid` | output | n/a (output) | yes |

**Gaps:**

- None found: every port is either driven with varying values or is an output sampled by at least one `Check`.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `grant_onehot0` | `$onehot0(grant)` | `clk` | `disable iff (!rst)` |
| `grant_reset_value` | `$rose(rst) |-> grant == 4'd0` | `clk` | none |

## Not Covered / Limitations

- Unweighted round-robin: every requester has equal priority in the rotation; there is no way to bias or prioritise a requester.
- No grant/acknowledge protocol: grant is asserted per the fixed latency of the chosen grant_style; the arbiter does not wait for an ack before rotating.
- N (4) is fixed at generation time: the priority network is unrolled for this exact requester count and is not a runtime parameter.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
- No `Check` samples cycle 0 (the first cycle after reset release): reset behavior runs structurally but is not directly self-checked by this TB.
