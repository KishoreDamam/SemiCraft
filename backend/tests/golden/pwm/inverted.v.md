# PWM Generator

A 8-bit PWM generator: a free-running counter compared against a runtime input (duty) produces pwm_out, inverted, with a 256-cycle period.

_Generated by SemiCraft 0.1.0 — config hash `be3c5ccd374e`._

## Ports

### Clocking

Clock and reset for the free-running period counter.

| Port | Direction | Description |
| --- | --- | --- |
| `clk` | input | Clock; the period counter increments on the rising edge. |
| `rst_n` | input | Sync reset, active-low reset input. |

### Data

Runtime duty-cycle threshold and the generated PWM output.

| Port | Direction | Description |
| --- | --- | --- |
| `duty` | input | 8-bit runtime duty-cycle threshold; the output is high while the counter is below this value. |
| `pwm_out` | output | PWM output, inverted (active-low); combinational comparison of cnt against duty (256-cycle period). |

## Configuration

- Resolution: 8 bits (period = 2^8 = 256 cycles)
- Duty-cycle source: a runtime input (duty)
- Output polarity: inverted (active-low)
- Reset: sync, active-low
- Output language: Verilog-2001

## Assumptions

- A single free-running clock drives the period counter.
- duty (when a runtime port) is stable while sampled by the combinational comparison; the caller is responsible for updating it synchronously if it varies.

## Limitations

- duty=0 makes the (uninverted) output constant low for the whole period (constant high when invert_output is set), since cnt is never less than 0.
- duty=255 (the maximum representable value) yields the output high for 255 of the 256 counter values — one cycle short of 100% duty, since the comparison is strict (cnt < duty), not cnt <= duty.
- No dead-time, phase-shift, or center-aligned modes; this is a single-channel, edge-aligned PWM only.
