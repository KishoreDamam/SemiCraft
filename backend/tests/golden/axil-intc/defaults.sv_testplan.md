# AXI4-Lite interrupt controller — Test Plan

An AXI4-Lite interrupt controller aggregating 8 edge-triggered source(s) into one masked request line. The AXI frontend is the register-block generator spliced in rather than instantiated, so the result is one flat module.

_Generated by SemiCraft 0.4.0 — config hash `aa30331fdec2`. Derived from `axil_intc`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_intc_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Sources: 8
- Trigger: edge
- Input synchroniser: 2 stages
- Registers: 3 (16 B decoded, 12 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

**Reset behavior:** The active-low sync reset clears PENDING and ENABLE, so the controller comes up with irq_out low and every source masked. The synchroniser clears too, so a line already high at reset release reads as a fresh request.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `bvalid`, `rvalid`, `irq_out` | cycle 0: no new stimulus this cycle (holds prior-driven values) | bvalid == 0; rvalid == 0; irq_out == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 4: expected value of `rvalid`, `rdata`, `rresp` | cycle 4: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 6: expected value of `rvalid`, `rdata`, `rresp` | cycle 6: drive arvalid=0 | rvalid == 1; rdata == 1; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 8: expected value of `rvalid`, `rdata`, `rresp` | cycle 8: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 11: expected value of `bvalid`, `bresp`, `irq_out` | cycle 11: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq_out == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 12: expected value of `bvalid` | cycle 12: drive araddr=8, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 13: expected value of `rvalid`, `rdata`, `rresp` | cycle 13: drive arvalid=0 | rvalid == 1; rdata == 1; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 21: expected value of `rvalid`, `rdata`, `rresp` | cycle 21: drive arvalid=0 | rvalid == 1; rdata == 3; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 23: expected value of `rvalid`, `rdata`, `rresp` | cycle 23: drive arvalid=0 | rvalid == 1; rdata == 1; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 26: expected value of `bvalid`, `bresp`, `irq_out` | cycle 26: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq_out == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 27: expected value of `bvalid` | cycle 27: drive araddr=8, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 28: expected value of `rvalid`, `rdata`, `rresp` | cycle 28: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 30: expected value of `rvalid`, `rdata`, `rresp` | cycle 30: drive arvalid=0 | rvalid == 1; rdata == 2; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 33: expected value of `bvalid`, `bresp`, `irq_out` | cycle 33: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq_out == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 34: expected value of `bvalid` | cycle 34: drive araddr=0, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 35: expected value of `rvalid`, `rdata`, `rresp` | cycle 35: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 38: expected value of `bvalid`, `bresp`, `irq_out` | cycle 38: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq_out == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 39: expected value of `bvalid` | cycle 39: drive irq_in=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 44: expected value of `rvalid`, `rdata`, `rresp` | cycle 44: drive arvalid=0 | rvalid == 1; rdata == 1; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 47: expected value of `bvalid`, `bresp`, `irq_out` | cycle 47: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq_out == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-21 | Cycle 48: expected value of `bvalid` | cycle 48: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-22 | Cycle 53: expected value of `rvalid`, `rdata`, `rresp` | cycle 53: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-23 | Cycle 56: expected value of `bvalid`, `bresp`, `irq_out` | cycle 56: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 2; irq_out == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-24 | Cycle 57: expected value of `bvalid` | cycle 57: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `aclk` — free-running, `#5` half-period.
- **Reset:** `areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 56 vector cycle(s) in `TbSpec.vectors`, extended to 58 cycle(s) because a `Check` targets a later cycle.
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `aclk` | clock | free-running | n/a |
| `areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `awaddr` | input | yes (varies across the run) | no |
| `awvalid` | input | yes (varies across the run) | no |
| `awready` | output | n/a (output) | no |
| `wdata` | input | yes (varies across the run) | no |
| `wstrb` | input | yes (varies across the run) | no |
| `wvalid` | input | yes (varies across the run) | no |
| `wready` | output | n/a (output) | no |
| `bresp` | output | n/a (output) | yes |
| `bvalid` | output | n/a (output) | yes |
| `bready` | input | yes (varies across the run) | no |
| `araddr` | input | yes (varies across the run) | no |
| `arvalid` | input | yes (varies across the run) | no |
| `arready` | output | n/a (output) | no |
| `rdata` | output | n/a (output) | yes |
| `rresp` | output | n/a (output) | yes |
| `rvalid` | output | n/a (output) | yes |
| `rready` | input | yes (varies across the run) | no |

#### Interrupts

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `irq_in` | input | yes (varies across the run) | no |
| `irq_out` | output | n/a (output) | yes |

**Gaps:**

- `awready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |
| `irq_idle_after_reset` | `$rose(areset) |-> irq_out == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- Flat priority: irq_out says that something is pending, not what. Software reads STATUS to find the source.
- No vectoring, no nesting, and no per-source priority.
- PENDING has one bit per source, so a second request arriving before the first is cleared is not counted.
- Edge-triggered: a source already high when the controller leaves reset produces one request, then nothing until it deasserts and rises again.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
