# AXI4-Lite interrupt controller

An AXI4-Lite interrupt controller aggregating 13 edge-triggered source(s) into one masked request line. The AXI frontend is the register-block generator spliced in rather than instantiated, so the result is one flat module.

_Generated by SemiCraft 0.3.0 — config hash `f2ebca9f537b`._

## Ports

### Clocking

Single AXI clock domain; reset is active-low per the spec.

| Port | Direction | Description |
| --- | --- | --- |
| `aclk` | input | AXI clock. |
| `areset_n` | input | AXI reset, active-low (the spec calls it ARESETn). |

### AXI4-Lite slave

AXI4-Lite target port (bundle `s_axil`).

| Port | Direction | Description |
| --- | --- | --- |
| `awaddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `awvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `awready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `wdata` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wstrb` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `araddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `arvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `arready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rdata` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rready` | output | AXI4-Lite signal; see the register block's datasheet. |

### Interrupts

Request lines in, one masked request out. Inputs are synchronised before they are latched.

| Port | Direction | Description |
| --- | --- | --- |
| `irq_in` | input | Asynchronous interrupt requests; a rising edge on a synchronised line sets that source's PENDING bit. |
| `irq_out` | output | High while any enabled source is pending; the OR of STATUS. |

## Register map

`INTC` — 32-bit data, 4-bit byte address (16 B decoded, 12 B used), 4-byte stride. Offsets are relative to the IP's base address.

| Offset | Name | Reset | Description |
| --- | --- | --- | --- |
| `0x00` | `PENDING` | `0x00000000` | Latched interrupt requests; write 1 to clear. |
| `0x04` | `ENABLE` | `0x00000000` | Per-source mask; 1 lets the source reach irq_out. |
| `0x08` | `STATUS` | `0x00000000` | PENDING & ENABLE — the sources actually requesting. |

Reset values are as *software* reads them, so a write-only (WO) field contributes zero.

### `PENDING` — offset `0x00`

Latched interrupt requests; write 1 to clear.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[12:0]` | `value` | W1C | `0x0000` | Latched interrupt requests; write 1 to clear. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `ENABLE` — offset `0x04`

Per-source mask; 1 lets the source reach irq_out.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[12:0]` | `value` | RW | `0x0000` | Per-source mask; 1 lets the source reach irq_out. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `STATUS` — offset `0x08`

PENDING & ENABLE — the sources actually requesting.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[12:0]` | `value` | RO | `0x0000` | PENDING & ENABLE — the sources actually requesting. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

## Bus interfaces

Each bundle below is a *naming* grouping of ordinary flat ports — the generated RTL declares them individually, exactly as listed. No SystemVerilog `interface` construct is emitted.

### `s_axil` — `axi4-lite` target

AXI4-Lite target serving PENDING/ENABLE/STATUS.

Timing: clocked by `aclk`, reset by `areset_n`.

| Protocol role | Port | Direction |
| --- | --- | --- |
| `awaddr` | `awaddr` | input |
| `awvalid` | `awvalid` | input |
| `awready` | `awready` | output |
| `wdata` | `wdata` | input |
| `wstrb` | `wstrb` | input |
| `wvalid` | `wvalid` | input |
| `wready` | `wready` | output |
| `bresp` | `bresp` | output |
| `bvalid` | `bvalid` | output |
| `bready` | `bready` | input |
| `araddr` | `araddr` | input |
| `arvalid` | `arvalid` | input |
| `arready` | `arready` | output |
| `rdata` | `rdata` | output |
| `rresp` | `rresp` | output |
| `rvalid` | `rvalid` | output |
| `rready` | `rready` | input |

## Configuration

- Sources: 13
- Trigger: edge
- Input synchroniser: 2 stages
- Registers: 3 (16 B decoded, 12 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

## Assumptions

- irq_in is asynchronous to aclk and is synchronised before use; a request takes 3 clocks to appear in PENDING.
- Sources are active-high.

## Limitations

- Flat priority: irq_out says that something is pending, not what. Software reads STATUS to find the source.
- No vectoring, no nesting, and no per-source priority.
- PENDING has one bit per source, so a second request arriving before the first is cleared is not counted.
- Edge-triggered: a source already high when the controller leaves reset produces one request, then nothing until it deasserts and rises again.
