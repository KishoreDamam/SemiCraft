# AXI4-Lite timer — Test Plan

An AXI4-Lite timer with a 32-bit down-counter and a 16-bit prescaler, running one-shot or periodically and raising a maskable interrupt on expiry. The AXI frontend is the register-block generator spliced in rather than instantiated, so the result is one flat module.

_Generated by SemiCraft 0.3.0 — config hash `820db2cdf78a`. Derived from `axil_timer`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_timer_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Counter: 32 bits
- Prescaler: 16 bits
- Period: (RELOAD + 1) * (PRESCALE + 1) clocks
- Registers: 5 (32 B decoded, 20 B used)
- Reset: async, active-low (fixed by AXI4-Lite)
- Output language: Verilog-2001

**Reset behavior:** The active-low async reset clears CTRL, so the timer comes up disabled with irq low, and clears RELOAD, PRESCALE and STATUS. COUNT resets to zero and is reloaded from RELOAD on the first clock, because the counter is held reloaded while disabled.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `bvalid`, `rvalid`, `irq` | cycle 0: no new stimulus this cycle (holds prior-driven values) | bvalid == 0; rvalid == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 3: expected value of `bvalid`, `bresp`, `irq` | cycle 3: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 4: expected value of `bvalid` | cycle 4: drive awaddr=4, awvalid=1, bready=1, wdata=3, wstrb=15, wvalid=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `bvalid`, `bresp`, `irq` | cycle 6: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `bvalid` | cycle 7: drive araddr=8, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 8: expected value of `rvalid`, `rdata`, `rresp` | cycle 8: drive arvalid=0 | rvalid == 1; rdata == 3; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 11: expected value of `bvalid`, `bresp`, `irq` | cycle 11: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 12: expected value of `bvalid` | cycle 12: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 19: expected value of `irq` | cycle 19: no new stimulus this cycle (holds prior-driven values) | irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 20: expected value of `irq` | cycle 20: no new stimulus this cycle (holds prior-driven values) | irq == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 22: expected value of `rvalid`, `rdata`, `rresp` | cycle 22: drive arvalid=0 | rvalid == 1; rdata == 1; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 25: expected value of `bvalid`, `bresp`, `irq` | cycle 25: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 26: expected value of `bvalid` | cycle 26: drive awaddr=16, awvalid=1, bready=1, wdata=1, wstrb=15, wvalid=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 28: expected value of `bvalid`, `bresp`, `irq` | cycle 28: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 29: expected value of `bvalid` | cycle 29: drive araddr=16, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 30: expected value of `rvalid`, `rdata`, `rresp` | cycle 30: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 33: expected value of `bvalid`, `bresp`, `irq` | cycle 33: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 34: expected value of `bvalid` | cycle 34: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 41: expected value of `irq` | cycle 41: no new stimulus this cycle (holds prior-driven values) | irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 42: expected value of `irq` | cycle 42: no new stimulus this cycle (holds prior-driven values) | irq == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-21 | Cycle 45: expected value of `bvalid`, `bresp`, `irq` | cycle 45: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-22 | Cycle 46: expected value of `bvalid` | cycle 46: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-23 | Cycle 71: expected value of `rvalid`, `rdata`, `rresp` | cycle 71: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-24 | Cycle 73: expected value of `rvalid`, `rdata`, `rresp`, `irq` | cycle 73: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-25 | Cycle 76: expected value of `bvalid`, `bresp`, `irq` | cycle 76: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 2; irq == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-26 | Cycle 77: expected value of `bvalid` | cycle 77: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `aclk` — free-running, `#5` half-period.
- **Reset:** `areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 76 vector cycle(s) in `TbSpec.vectors`, extended to 78 cycle(s) because a `Check` targets a later cycle.
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `aclk` | clock | free-running | n/a |
| `areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `awaddr` | input | yes (varies across the run) | no |
| `awvalid` | input | yes (varies across the run) | no |
| `awready` | output | n/a (output) | no |
| `wdata` | input | yes (varies across the run) | no |
| `wstrb` | input | yes (varies across the run) | no |
| `wvalid` | input | yes (varies across the run) | no |
| `wready` | output | n/a (output) | no |
| `bresp` | output | n/a (output) | yes |
| `bvalid` | output | n/a (output) | yes |
| `bready` | input | yes (varies across the run) | no |
| `araddr` | input | yes (varies across the run) | no |
| `arvalid` | input | yes (varies across the run) | no |
| `arready` | output | n/a (output) | no |
| `rdata` | output | n/a (output) | yes |
| `rresp` | output | n/a (output) | yes |
| `rvalid` | output | n/a (output) | yes |
| `rready` | input | yes (varies across the run) | no |

#### Interrupt

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `irq` | output | n/a (output) | yes |

**Gaps:**

- `awready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |
| `irq_idle_after_reset` | `$rose(areset) |-> irq == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- No pause and resume: clearing CTRL.enable reloads the counter rather than freezing it.
- No capture or compare channels, and no free-running mode — COUNT counts down to zero and is not writable.
- STATUS.expired does not count missed expiries: a second expiry before software clears the flag is indistinguishable from the first.
- irq rises two clocks after the counter reaches its terminal count (one to sample the set request, one for the flag to be readable).
- Bits above counter_width are reserved in RELOAD and COUNT, and bits above 2 in CTRL; writing one returns SLVERR.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
