# AXI4-Lite GPIO — Test Plan

An AXI4-Lite GPIO controller for 8 pin(s). The AXI frontend is the same register-block generator the standalone axil-regblock IP uses, spliced into this module rather than instantiated, so the result is one flat module.

_Generated by SemiCraft 0.4.0 — config hash `28458bc0a6db`. Derived from `axil_gpio`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_gpio_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Pins: 8
- Input synchroniser: 2 stages
- Registers: 3 (16 B decoded, 12 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

**Reset behavior:** The active-low sync reset clears DIR and OUT, so every pin comes up as an input driving zero and the block cannot fight whatever is already on the pad. The input synchroniser clears too, so IN reads zero until the pins have been sampled.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `p_bvalid`, `p_rvalid`, `p_awready`, `p_gpioOe`, `p_gpioOut` | cycle 0: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 0; p_rvalid == 0; p_awready == 1; p_gpioOe == 0; p_gpioOut == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 3: expected value of `p_bvalid`, `p_bresp`, `p_gpioOe` | cycle 3: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_gpioOe == 255 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 4: expected value of `p_bvalid` | cycle 4: drive p_awaddr=4, p_awvalid=1, p_bready=1, p_wdata=165, p_wstrb=15, p_wvalid=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `p_bvalid`, `p_bresp`, `p_gpioOut` | cycle 6: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_gpioOut == 165 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `p_bvalid` | cycle 7: drive p_araddr=0, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 8: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 8: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 255; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 11: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 11: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 0; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 15: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 15: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 90; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 18: expected value of `p_bvalid`, `p_bresp`, `p_gpioOe` | cycle 18: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 2; p_gpioOe == 255 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 19: expected value of `p_bvalid` | cycle 19: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `p_aclk` — free-running, `#5` half-period.
- **Reset:** `p_areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 18 vector cycle(s) in `TbSpec.vectors`, extended to 20 cycle(s) because a `Check` targets a later cycle.
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_aclk` | clock | free-running | n/a |
| `p_areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_awaddr` | input | yes (varies across the run) | no |
| `p_awvalid` | input | yes (varies across the run) | no |
| `p_awready` | output | n/a (output) | yes |
| `p_wdata` | input | yes (varies across the run) | no |
| `p_wstrb` | input | yes (varies across the run) | no |
| `p_wvalid` | input | yes (varies across the run) | no |
| `p_wready` | output | n/a (output) | no |
| `p_bresp` | output | n/a (output) | yes |
| `p_bvalid` | output | n/a (output) | yes |
| `p_bready` | input | yes (varies across the run) | no |
| `p_araddr` | input | yes (varies across the run) | no |
| `p_arvalid` | input | yes (varies across the run) | no |
| `p_arready` | output | n/a (output) | no |
| `p_rdata` | output | n/a (output) | yes |
| `p_rresp` | output | n/a (output) | yes |
| `p_rvalid` | output | n/a (output) | yes |
| `p_rready` | input | yes (varies across the run) | no |

#### Pins

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_gpioIn` | input | yes (varies across the run) | no |
| `p_gpioOut` | output | n/a (output) | yes |
| `p_gpioOe` | output | n/a (output) | yes |

**Gaps:**

- `p_wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `p_arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |
| `pins_not_driven_after_reset` | `$rose(areset) |-> gpio_oe == 8'd0` | `aclk` | none |

## Not Covered / Limitations

- No interrupt on pin change: IN must be polled.
- No per-pin open-drain or pull configuration.
- No bidirectional port — the IR has no `inout`, so direction is exposed as a separate gpio_oe output.
- Bits above num_pins are reserved in every register; writing one returns SLVERR (the register block's standard policy).
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
