# AXI4-Lite GPIO

An AXI4-Lite GPIO controller for 8 pin(s). The AXI frontend is the same register-block generator the standalone axil-regblock IP uses, spliced into this module rather than instantiated, so the result is one flat module.

_Generated by SemiCraft 0.4.0 — config hash `28458bc0a6db`._

## Ports

### Clocking

Single AXI clock domain; reset is active-low per the spec.

| Port | Direction | Description |
| --- | --- | --- |
| `p_aclk` | input | AXI clock. |
| `p_areset_n` | input | AXI reset, active-low (the spec calls it ARESETn). |

### AXI4-Lite slave

AXI4-Lite target port (bundle `s_axil`).

| Port | Direction | Description |
| --- | --- | --- |
| `p_awaddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_awvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_awready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_wdata` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_wstrb` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_wvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_wready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_bresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_bvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_bready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_araddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_arvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_arready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rdata` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rready` | output | AXI4-Lite signal; see the register block's datasheet. |

### Pins

Pad-facing signals. There is no bidirectional net: the output enable is separate so the integrator instantiates the tri-state buffer.

| Port | Direction | Description |
| --- | --- | --- |
| `p_gpioIn` | input | Asynchronous pin inputs; synchronised through 2 flip-flops before reaching IN. |
| `p_gpioOut` | output | Pin output values, straight from the OUT register. |
| `p_gpioOe` | output | Pin output enables, straight from the DIR register. |

## Register map

`GPIO` — 32-bit data, 4-bit byte address (16 B decoded, 12 B used), 4-byte stride. Offsets are relative to the IP's base address.

| Offset | Name | Reset | Description |
| --- | --- | --- | --- |
| `0x00` | `DIR` | `0x00000000` | Per-pin direction; 1 drives the pin. |
| `0x04` | `OUT` | `0x00000000` | Per-pin output value, driven where DIR is 1. |
| `0x08` | `IN` | `0x00000000` | Synchronised pin input. |

Reset values are as *software* reads them, so a write-only (WO) field contributes zero.

### `DIR` — offset `0x00`

Per-pin direction; 1 drives the pin.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[7:0]` | `value` | RW | `0x00` | Per-pin direction; 1 drives the pin. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `OUT` — offset `0x04`

Per-pin output value, driven where DIR is 1.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[7:0]` | `value` | RW | `0x00` | Per-pin output value, driven where DIR is 1. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `IN` — offset `0x08`

Synchronised pin input.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[7:0]` | `value` | RO | `0x00` | Synchronised pin input. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

## Bus interfaces

Each bundle below is a *naming* grouping of ordinary flat ports — the generated RTL declares them individually, exactly as listed. No SystemVerilog `interface` construct is emitted.

### `s_axil` — `axi4-lite` target

AXI4-Lite target serving DIR/OUT/IN.

Timing: clocked by `p_aclk`, reset by `p_areset_n`.

| Protocol role | Port | Direction |
| --- | --- | --- |
| `awaddr` | `p_awaddr` | input |
| `awvalid` | `p_awvalid` | input |
| `awready` | `p_awready` | output |
| `wdata` | `p_wdata` | input |
| `wstrb` | `p_wstrb` | input |
| `wvalid` | `p_wvalid` | input |
| `wready` | `p_wready` | output |
| `bresp` | `p_bresp` | output |
| `bvalid` | `p_bvalid` | output |
| `bready` | `p_bready` | input |
| `araddr` | `p_araddr` | input |
| `arvalid` | `p_arvalid` | input |
| `arready` | `p_arready` | output |
| `rdata` | `p_rdata` | output |
| `rresp` | `p_rresp` | output |
| `rvalid` | `p_rvalid` | output |
| `rready` | `p_rready` | input |

## Timing

Rendered from the same directed testbench recipe the smoke simulation runs in CI, so this diagram cannot drift from the behaviour the tests verify.

Inputs are shown as the testbench drives them. Outputs are shown only on the cycles it checks them; `x` means the testbench pins no value there, not that the signal is undefined in hardware.

```wavedrom
{
  "signal": [
    {
      "name": "p_aclk",
      "wave": "p....................."
    },
    {
      "name": "p_areset_n",
      "wave": "0.1..................."
    },
    {},
    {
      "name": "p_awaddr",
      "wave": "=.....=...........=...",
      "data": [
        "0x0",
        "0x4",
        "0x0"
      ]
    },
    {
      "name": "p_awvalid",
      "wave": "0..10.10..........10.."
    },
    {
      "name": "p_wdata",
      "wave": "=..=..=...........=...",
      "data": [
        "0x0",
        "0xFF",
        "0xA5",
        "0x100"
      ]
    },
    {
      "name": "p_wstrb",
      "wave": "=..=..................",
      "data": [
        "0x0",
        "0xF"
      ]
    },
    {
      "name": "p_wvalid",
      "wave": "0..10.10..........10.."
    },
    {
      "name": "p_bready",
      "wave": "0..1.................."
    },
    {
      "name": "p_araddr",
      "wave": "=...........=.........",
      "data": [
        "0x0",
        "0x8"
      ]
    },
    {
      "name": "p_arvalid",
      "wave": "0........10.10..10...."
    },
    {
      "name": "p_rready",
      "wave": "0........1............"
    },
    {
      "name": "p_gpioIn",
      "wave": "=..........=..........",
      "data": [
        "0x0",
        "0x5A"
      ]
    },
    {},
    {
      "name": "p_awready",
      "wave": "xx1xxxxxxxxxxxxxxxxxxx"
    },
    {
      "name": "p_bresp",
      "wave": "xxxxx=xx=xxxxxxxxxxx=x",
      "data": [
        "0x0",
        "0x0",
        "0x2"
      ]
    },
    {
      "name": "p_bvalid",
      "wave": "xx0xx10x10xxxxxxxxxx10"
    },
    {
      "name": "p_rdata",
      "wave": "xxxxxxxxxx=xx=xxx=xxxx",
      "data": [
        "0xFF",
        "0x0",
        "0x5A"
      ]
    },
    {
      "name": "p_rresp",
      "wave": "xxxxxxxxxx=xx=xxx=xxxx",
      "data": [
        "0x0",
        "0x0",
        "0x0"
      ]
    },
    {
      "name": "p_rvalid",
      "wave": "xx0xxxxxxx1xx1xxx1xxxx"
    },
    {
      "name": "p_gpioOut",
      "wave": "xx=xxxxx=xxxxxxxxxxxxx",
      "data": [
        "0x0",
        "0xA5"
      ]
    },
    {
      "name": "p_gpioOe",
      "wave": "xx=xx=xxxxxxxxxxxxxx=x",
      "data": [
        "0x0",
        "0xFF",
        "0xFF"
      ]
    }
  ],
  "head": {
    "text": "axil_gpio \u2014 directed sequence",
    "tick": 0
  },
  "config": {
    "hscale": 1
  }
}
```

## Configuration

- Pins: 8
- Input synchroniser: 2 stages
- Registers: 3 (16 B decoded, 12 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

## Assumptions

- gpio_in is asynchronous to aclk and is synchronised before use; a pin change takes input_sync_stages clocks to appear in IN.
- The integrator instantiates the tri-state buffer, driving the pad from gpio_out when gpio_oe is high.

## Limitations

- No interrupt on pin change: IN must be polled.
- No per-pin open-drain or pull configuration.
- No bidirectional port — the IR has no `inout`, so direction is exposed as a separate gpio_oe output.
- Bits above num_pins are reserved in every register; writing one returns SLVERR (the register block's standard policy).
