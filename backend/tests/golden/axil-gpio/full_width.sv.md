# AXI4-Lite GPIO

An AXI4-Lite GPIO controller for 32 pin(s). The AXI frontend is the same register-block generator the standalone axil-regblock IP uses, spliced into this module rather than instantiated, so the result is one flat module.

_Generated by SemiCraft 0.3.0 — config hash `b90e27f8c418`._

## Ports

### Clocking

Single AXI clock domain; reset is active-low per the spec.

| Port | Direction | Description |
| --- | --- | --- |
| `aclk` | input | AXI clock. |
| `areset_n` | input | AXI reset, active-low (the spec calls it ARESETn). |

### AXI4-Lite slave

AXI4-Lite target port (bundle `s_axil`).

| Port | Direction | Description |
| --- | --- | --- |
| `awaddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `awvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `awready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `wdata` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wstrb` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `araddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `arvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `arready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rdata` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rready` | output | AXI4-Lite signal; see the register block's datasheet. |

### Pins

Pad-facing signals. There is no bidirectional net: the output enable is separate so the integrator instantiates the tri-state buffer.

| Port | Direction | Description |
| --- | --- | --- |
| `gpio_in` | input | Asynchronous pin inputs; synchronised through 2 flip-flops before reaching IN. |
| `gpio_out` | output | Pin output values, straight from the OUT register. |
| `gpio_oe` | output | Pin output enables, straight from the DIR register. |

## Register map

`GPIO` — 32-bit data, 4-bit byte address (16 B decoded, 12 B used), 4-byte stride. Offsets are relative to the IP's base address.

| Offset | Name | Reset | Description |
| --- | --- | --- | --- |
| `0x00` | `DIR` | `0x00000000` | Per-pin direction; 1 drives the pin. |
| `0x04` | `OUT` | `0x00000000` | Per-pin output value, driven where DIR is 1. |
| `0x08` | `IN` | `0x00000000` | Synchronised pin input. |

Reset values are as *software* reads them, so a write-only (WO) field contributes zero.

### `DIR` — offset `0x00`

Per-pin direction; 1 drives the pin.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[31:0]` | `value` | RW | `0x00000000` | Per-pin direction; 1 drives the pin. |

### `OUT` — offset `0x04`

Per-pin output value, driven where DIR is 1.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[31:0]` | `value` | RW | `0x00000000` | Per-pin output value, driven where DIR is 1. |

### `IN` — offset `0x08`

Synchronised pin input.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[31:0]` | `value` | RO | `0x00000000` | Synchronised pin input. |

## Bus interfaces

Each bundle below is a *naming* grouping of ordinary flat ports — the generated RTL declares them individually, exactly as listed. No SystemVerilog `interface` construct is emitted.

### `s_axil` — `axi4-lite` target

AXI4-Lite target serving DIR/OUT/IN.

Timing: clocked by `aclk`, reset by `areset_n`.

| Protocol role | Port | Direction |
| --- | --- | --- |
| `awaddr` | `awaddr` | input |
| `awvalid` | `awvalid` | input |
| `awready` | `awready` | output |
| `wdata` | `wdata` | input |
| `wstrb` | `wstrb` | input |
| `wvalid` | `wvalid` | input |
| `wready` | `wready` | output |
| `bresp` | `bresp` | output |
| `bvalid` | `bvalid` | output |
| `bready` | `bready` | input |
| `araddr` | `araddr` | input |
| `arvalid` | `arvalid` | input |
| `arready` | `arready` | output |
| `rdata` | `rdata` | output |
| `rresp` | `rresp` | output |
| `rvalid` | `rvalid` | output |
| `rready` | `rready` | input |

## Configuration

- Pins: 32
- Input synchroniser: 2 stages
- Registers: 3 (16 B decoded, 12 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

## Assumptions

- gpio_in is asynchronous to aclk and is synchronised before use; a pin change takes input_sync_stages clocks to appear in IN.
- The integrator instantiates the tri-state buffer, driving the pad from gpio_out when gpio_oe is high.

## Limitations

- No interrupt on pin change: IN must be polled.
- No per-pin open-drain or pull configuration.
- No bidirectional port — the IR has no `inout`, so direction is exposed as a separate gpio_oe output.
- Bits above num_pins are reserved in every register; writing one returns SLVERR (the register block's standard policy).
