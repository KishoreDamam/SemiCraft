# AXI4-Lite GPIO — Test Plan

An AXI4-Lite GPIO controller for 8 pin(s). The AXI frontend is the same register-block generator the standalone axil-regblock IP uses, spliced into this module rather than instantiated, so the result is one flat module.

_Generated by SemiCraft 0.4.0 — config hash `56f8c43264a3`. Derived from `axil_gpio`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_gpio_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Pins: 8
- Input synchroniser: 2 stages
- Registers: 3 (16 B decoded, 12 B used)
- Reset: async, active-low (fixed by AXI4-Lite)
- Output language: Verilog-2001

**Reset behavior:** The active-low async reset clears DIR and OUT, so every pin comes up as an input driving zero and the block cannot fight whatever is already on the pad. The input synchroniser clears too, so IN reads zero until the pins have been sampled.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `bvalid`, `rvalid`, `awready`, `gpio_oe`, `gpio_out` | cycle 0: no new stimulus this cycle (holds prior-driven values) | bvalid == 0; rvalid == 0; awready == 1; gpio_oe == 0; gpio_out == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 3: expected value of `bvalid`, `bresp`, `gpio_oe` | cycle 3: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; gpio_oe == 255 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 4: expected value of `bvalid` | cycle 4: drive awaddr=4, awvalid=1, bready=1, wdata=165, wstrb=15, wvalid=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `bvalid`, `bresp`, `gpio_out` | cycle 6: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; gpio_out == 165 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `bvalid` | cycle 7: drive araddr=0, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 8: expected value of `rvalid`, `rdata`, `rresp` | cycle 8: drive arvalid=0 | rvalid == 1; rdata == 255; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 11: expected value of `rvalid`, `rdata`, `rresp` | cycle 11: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 15: expected value of `rvalid`, `rdata`, `rresp` | cycle 15: drive arvalid=0 | rvalid == 1; rdata == 90; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 18: expected value of `bvalid`, `bresp`, `gpio_oe` | cycle 18: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 2; gpio_oe == 255 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 19: expected value of `bvalid` | cycle 19: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `aclk` — free-running, `#5` half-period.
- **Reset:** `areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 18 vector cycle(s) in `TbSpec.vectors`, extended to 20 cycle(s) because a `Check` targets a later cycle.
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `aclk` | clock | free-running | n/a |
| `areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `awaddr` | input | yes (varies across the run) | no |
| `awvalid` | input | yes (varies across the run) | no |
| `awready` | output | n/a (output) | yes |
| `wdata` | input | yes (varies across the run) | no |
| `wstrb` | input | yes (varies across the run) | no |
| `wvalid` | input | yes (varies across the run) | no |
| `wready` | output | n/a (output) | no |
| `bresp` | output | n/a (output) | yes |
| `bvalid` | output | n/a (output) | yes |
| `bready` | input | yes (varies across the run) | no |
| `araddr` | input | yes (varies across the run) | no |
| `arvalid` | input | yes (varies across the run) | no |
| `arready` | output | n/a (output) | no |
| `rdata` | output | n/a (output) | yes |
| `rresp` | output | n/a (output) | yes |
| `rvalid` | output | n/a (output) | yes |
| `rready` | input | yes (varies across the run) | no |

#### Pins

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `gpio_in` | input | yes (varies across the run) | no |
| `gpio_out` | output | n/a (output) | yes |
| `gpio_oe` | output | n/a (output) | yes |

**Gaps:**

- `wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |
| `pins_not_driven_after_reset` | `$rose(areset) |-> gpio_oe == 8'd0` | `aclk` | none |

## Not Covered / Limitations

- No interrupt on pin change: IN must be polled.
- No per-pin open-drain or pull configuration.
- No bidirectional port — the IR has no `inout`, so direction is exposed as a separate gpio_oe output.
- Bits above num_pins are reserved in every register; writing one returns SLVERR (the register block's standard policy).
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
