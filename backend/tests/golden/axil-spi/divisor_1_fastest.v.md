# AXI4-Lite SPI Master

An AXI4-Lite SPI master in mode 00. Writing TXDATA performs one full-duplex 8-bit exchange: a byte goes out on mosi while a byte comes in on miso. The AXI frontend is the register-block generator spliced into this module.

_Generated by SemiCraft 0.3.0 — config hash `073e4516e3a3`._

## Ports

### Clocking

Single AXI clock domain; reset is active-low per the spec.

| Port | Direction | Description |
| --- | --- | --- |
| `aclk` | input | AXI clock. |
| `areset_n` | input | AXI reset, active-low (the spec calls it ARESETn). |

### AXI4-Lite slave

AXI4-Lite target port (bundle `s_axil`).

| Port | Direction | Description |
| --- | --- | --- |
| `awaddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `awvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `awready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `wdata` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wstrb` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `wready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `bready` | input | AXI4-Lite signal; see the register block's datasheet. |
| `araddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `arvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `arready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rdata` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `rready` | input | AXI4-Lite signal; see the register block's datasheet. |

### SPI link

Master-side SPI signals (bundle `spi`).

| Port | Direction | Description |
| --- | --- | --- |
| `sclk` | output | Serial clock; idles at 0 (CPOL). |
| `mosi` | output | Data to the slave, MSB first. |
| `miso` | input | Data from the slave, sampled through 2 synchroniser flops. |
| `cs_n` | output | Chip select, active low; driven directly by CTRL.cs_assert. |

## Register map

`SPI` — 32-bit data, 5-bit byte address (32 B decoded, 20 B used), 4-byte stride. Offsets are relative to the IP's base address.

| Offset | Name | Reset | Description |
| --- | --- | --- | --- |
| `0x00` | `STATUS` | `0x00000000` | Transfer status. |
| `0x04` | `CTRL` | `0x00000000` | Chip select control. |
| `0x08` | `TXDATA` | `0x00000000` | Writing this register starts one 8-bit exchange. |
| `0x0C` | `RXDATA` | `0x00000000` | The byte shifted in during the last exchange. |
| `0x10` | `CLKDIV` | `0x00000001` | sclk half period in aclk cycles. |

Reset values are as *software* reads them, so a write-only (WO) field contributes zero.

### `STATUS` — offset `0x00`

Transfer status.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[1]` | `rx_valid` | W1C | `0x0` | RXDATA holds a received byte; write 1 to clear. |
| `[0]` | `busy` | RO | `0x0` | An exchange is in progress. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `CTRL` — offset `0x04`

Chip select control.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[0]` | `cs_assert` | RW | `0x0` | Drive cs_n low. Held across as many exchanges as wanted. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `TXDATA` — offset `0x08`

Writing this register starts one 8-bit exchange.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[7:0]` | `data` | WO | `0x00` | Byte to shift out, MSB first; reads back as zero. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `RXDATA` — offset `0x0C`

The byte shifted in during the last exchange.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[7:0]` | `data` | RO | `0x00` | Received byte; valid while STATUS.rx_valid is set. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `CLKDIV` — offset `0x10`

sclk half period in aclk cycles.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[15:0]` | `div` | RW | `0x0001` | Half period; the sclk period is twice this. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

## Bus interfaces

Each bundle below is a *naming* grouping of ordinary flat ports — the generated RTL declares them individually, exactly as listed. No SystemVerilog `interface` construct is emitted.

### `s_axil` — `axi4-lite` target

AXI4-Lite target serving the SPI registers.

Timing: clocked by `aclk`, reset by `areset_n`.

| Protocol role | Port | Direction |
| --- | --- | --- |
| `awaddr` | `awaddr` | input |
| `awvalid` | `awvalid` | input |
| `awready` | `awready` | output |
| `wdata` | `wdata` | input |
| `wstrb` | `wstrb` | input |
| `wvalid` | `wvalid` | input |
| `wready` | `wready` | output |
| `bresp` | `bresp` | output |
| `bvalid` | `bvalid` | output |
| `bready` | `bready` | input |
| `araddr` | `araddr` | input |
| `arvalid` | `arvalid` | input |
| `arready` | `arready` | output |
| `rdata` | `rdata` | output |
| `rresp` | `rresp` | output |
| `rvalid` | `rvalid` | output |
| `rready` | `rready` | input |

### `spi` — `spi` initiator

SPI link; this IP drives the clock and chip select.

Timing: clocked by `aclk`, reset by `areset_n`.

| Protocol role | Port | Direction |
| --- | --- | --- |
| `sclk` | `sclk` | output |
| `mosi` | `mosi` | output |
| `miso` | `miso` | input |
| `cs_n` | `cs_n` | output |

## Configuration

- SPI mode: 00 (CPOL=0, CPHA=0)
- Reset clock divisor: 1 aclk cycles per sclk half period (CLKDIV is writable at run time)
- Input synchroniser: 2 stages
- Registers: 5 (32 B decoded, 20 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: Verilog-2001

## Assumptions

- The slave uses the same mode; CPOL/CPHA are fixed at generate time.
- Software holds CTRL.cs_assert across a multi-byte transaction and releases it afterwards — the exchange itself never touches cs_n.
- miso is sampled through a synchroniser, so it need not be phase-aligned to aclk.
- Software writes TXDATA only while STATUS.busy is clear.

## Limitations

- 8-bit transfers only, MSB first: no configurable word length and no LSB-first mode.
- CPOL/CPHA are generate-time options, not register bits — a runtime mode switch would carry both edge behaviours in hardware forever.
- Single chip select. Multiple slaves need external decoding of cs_n.
- No FIFOs: one byte in flight each way, and STATUS.rx_valid must be cleared between exchanges.
- No interrupt output; STATUS must be polled.
