# AXI4-Lite SPI Master

An AXI4-Lite SPI master in mode 00. Writing TXDATA performs one full-duplex 8-bit exchange: a byte goes out on mosi while a byte comes in on miso. The AXI frontend is the register-block generator spliced into this module.

_Generated by SemiCraft 0.4.0 — config hash `bea2ab0054b1`._

## Ports

### Clocking

Single AXI clock domain; reset is active-low per the spec.

| Port | Direction | Description |
| --- | --- | --- |
| `p_aclk` | input | AXI clock. |
| `p_areset_n` | input | AXI reset, active-low (the spec calls it ARESETn). |

### AXI4-Lite slave

AXI4-Lite target port (bundle `s_axil`).

| Port | Direction | Description |
| --- | --- | --- |
| `p_awaddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_awvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_awready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_wdata` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_wstrb` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_wvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_wready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_bresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_bvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_bready` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_araddr` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_arvalid` | input | AXI4-Lite signal; see the register block's datasheet. |
| `p_arready` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rdata` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rresp` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rvalid` | output | AXI4-Lite signal; see the register block's datasheet. |
| `p_rready` | input | AXI4-Lite signal; see the register block's datasheet. |

### SPI link

Master-side SPI signals (bundle `spi`).

| Port | Direction | Description |
| --- | --- | --- |
| `p_sclk` | output | Serial clock; idles at 0 (CPOL). |
| `p_mosi` | output | Data to the slave, MSB first. |
| `p_miso` | input | Data from the slave, sampled through 2 synchroniser flops. |
| `p_csN` | output | Chip select, active low; driven directly by CTRL.cs_assert. |

## Register map

`SPI` — 32-bit data, 5-bit byte address (32 B decoded, 20 B used), 4-byte stride. Offsets are relative to the IP's base address.

| Offset | Name | Reset | Description |
| --- | --- | --- | --- |
| `0x00` | `STATUS` | `0x00000000` | Transfer status. |
| `0x04` | `CTRL` | `0x00000000` | Chip select control. |
| `0x08` | `TXDATA` | `0x00000000` | Writing this register starts one 8-bit exchange. |
| `0x0C` | `RXDATA` | `0x00000000` | The byte shifted in during the last exchange. |
| `0x10` | `CLKDIV` | `0x00000002` | sclk half period in aclk cycles. |

Reset values are as *software* reads them, so a write-only (WO) field contributes zero.

### `STATUS` — offset `0x00`

Transfer status.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[1]` | `rx_valid` | W1C | `0x0` | RXDATA holds a received byte; write 1 to clear. |
| `[0]` | `busy` | RO | `0x0` | An exchange is in progress. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `CTRL` — offset `0x04`

Chip select control.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[0]` | `cs_assert` | RW | `0x0` | Drive cs_n low. Held across as many exchanges as wanted. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `TXDATA` — offset `0x08`

Writing this register starts one 8-bit exchange.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[7:0]` | `data` | WO | `0x00` | Byte to shift out, MSB first; reads back as zero. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `RXDATA` — offset `0x0C`

The byte shifted in during the last exchange.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[7:0]` | `data` | RO | `0x00` | Received byte; valid while STATUS.rx_valid is set. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

### `CLKDIV` — offset `0x10`

sclk half period in aclk cycles.

| Bits | Field | Access | Reset | Description |
| --- | --- | --- | --- | --- |
| `[15:0]` | `div` | RW | `0x0002` | Half period; the sclk period is twice this. |

Bits not listed above are reserved and read as zero. What a write to them does is the register block's policy — see the IP's own documentation.

## Bus interfaces

Each bundle below is a *naming* grouping of ordinary flat ports — the generated RTL declares them individually, exactly as listed. No SystemVerilog `interface` construct is emitted.

### `s_axil` — `axi4-lite` target

AXI4-Lite target serving the SPI registers.

Timing: clocked by `p_aclk`, reset by `p_areset_n`.

| Protocol role | Port | Direction |
| --- | --- | --- |
| `awaddr` | `p_awaddr` | input |
| `awvalid` | `p_awvalid` | input |
| `awready` | `p_awready` | output |
| `wdata` | `p_wdata` | input |
| `wstrb` | `p_wstrb` | input |
| `wvalid` | `p_wvalid` | input |
| `wready` | `p_wready` | output |
| `bresp` | `p_bresp` | output |
| `bvalid` | `p_bvalid` | output |
| `bready` | `p_bready` | input |
| `araddr` | `p_araddr` | input |
| `arvalid` | `p_arvalid` | input |
| `arready` | `p_arready` | output |
| `rdata` | `p_rdata` | output |
| `rresp` | `p_rresp` | output |
| `rvalid` | `p_rvalid` | output |
| `rready` | `p_rready` | input |

### `spi` — `spi` initiator

SPI link; this IP drives the clock and chip select.

Timing: clocked by `p_aclk`, reset by `p_areset_n`.

| Protocol role | Port | Direction |
| --- | --- | --- |
| `sclk` | `p_sclk` | output |
| `mosi` | `p_mosi` | output |
| `miso` | `p_miso` | input |
| `cs_n` | `p_csN` | output |

## Timing

Rendered from the same directed testbench recipe the smoke simulation runs in CI, so this diagram cannot drift from the behaviour the tests verify.

Inputs are shown as the testbench drives them. Outputs are shown only on the cycles it checks them; `x` means the testbench pins no value there, not that the signal is undefined in hardware.

Showing the first 40 of 53 directed cycles.

```wavedrom
{
  "signal": [
    {
      "name": "p_aclk",
      "wave": "p........................................."
    },
    {
      "name": "p_areset_n",
      "wave": "0.1......................................."
    },
    {},
    {
      "name": "p_awaddr",
      "wave": "=.=...=...................................",
      "data": [
        "0x0",
        "0x4",
        "0x8"
      ]
    },
    {
      "name": "p_awvalid",
      "wave": "0.10..10.................................."
    },
    {
      "name": "p_wdata",
      "wave": "=.=...=...................................",
      "data": [
        "0x0",
        "0x1",
        "0x4B"
      ]
    },
    {
      "name": "p_wstrb",
      "wave": "=.=.......................................",
      "data": [
        "0x0",
        "0xF"
      ]
    },
    {
      "name": "p_wvalid",
      "wave": "0.10..10.................................."
    },
    {
      "name": "p_bready",
      "wave": "0.1......................................."
    },
    {
      "name": "p_araddr",
      "wave": "=.........................................",
      "data": [
        "0x0"
      ]
    },
    {
      "name": "p_arvalid",
      "wave": "0........................................."
    },
    {
      "name": "p_rready",
      "wave": "0........................................."
    },
    {
      "name": "p_miso",
      "wave": "0...............1...0...1.......0...1....."
    },
    {},
    {
      "name": "p_bresp",
      "wave": "xxxx=xxx=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
      "data": [
        "0x0",
        "0x0"
      ]
    },
    {
      "name": "p_bvalid",
      "wave": "xx0x10xx10xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    },
    {
      "name": "p_rdata",
      "wave": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    },
    {
      "name": "p_rresp",
      "wave": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    },
    {
      "name": "p_rvalid",
      "wave": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    },
    {
      "name": "p_sclk",
      "wave": "xx0xxxxxxxx1x0xxxxxxxxxxxxxxxxxxxxxxxxxxx0"
    },
    {
      "name": "p_mosi",
      "wave": "xxxxxxxxxx0xxx1xxx0xxx0xxx1xxx0xxx1xxx1xxx"
    },
    {
      "name": "p_csN",
      "wave": "xx1x0xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    }
  ],
  "head": {
    "text": "axil_spi \u2014 directed sequence",
    "tick": 0
  },
  "config": {
    "hscale": 1
  }
}
```

## Configuration

- SPI mode: 00 (CPOL=0, CPHA=0)
- Reset clock divisor: 2 aclk cycles per sclk half period (CLKDIV is writable at run time)
- Input synchroniser: 2 stages
- Registers: 5 (32 B decoded, 20 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

## Assumptions

- The slave uses the same mode; CPOL/CPHA are fixed at generate time.
- Software holds CTRL.cs_assert across a multi-byte transaction and releases it afterwards — the exchange itself never touches cs_n.
- miso is sampled through a synchroniser, so it need not be phase-aligned to aclk.
- Software writes TXDATA only while STATUS.busy is clear.

## Limitations

- 8-bit transfers only, MSB first: no configurable word length and no LSB-first mode.
- CPOL/CPHA are generate-time options, not register bits — a runtime mode switch would carry both edge behaviours in hardware forever.
- Single chip select. Multiple slaves need external decoding of cs_n.
- No FIFOs: one byte in flight each way, and STATUS.rx_valid must be cleared between exchanges.
- No interrupt output; STATUS must be polled.
