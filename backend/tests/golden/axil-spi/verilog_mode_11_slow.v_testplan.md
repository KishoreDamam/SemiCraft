# AXI4-Lite SPI Master — Test Plan

An AXI4-Lite SPI master in mode 11. Writing TXDATA performs one full-duplex 8-bit exchange: a byte goes out on mosi while a byte comes in on miso. The AXI frontend is the register-block generator spliced into this module.

_Generated by SemiCraft 0.3.0 — config hash `48d96e5ea16b`. Derived from `axil_spi`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_spi_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- SPI mode: 11 (CPOL=1, CPHA=1)
- Reset clock divisor: 10 aclk cycles per sclk half period (CLKDIV is writable at run time)
- Input synchroniser: 2 stages
- Registers: 5 (32 B decoded, 20 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: Verilog-2001

**Reset behavior:** The active-low sync reset returns the transfer FSM to idle, puts sclk back to its idle level (1) and releases cs_n, so the master cannot be left driving a slave's bus after a reset. CLKDIV returns to its reset divisor and CTRL.cs_assert clears.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `sclk`, `cs_n`, `bvalid` | cycle 0: drive awaddr=4, awvalid=1, bready=1, wdata=1, wstrb=15, wvalid=1 | sclk == 1; cs_n == 1; bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 2: expected value of `bvalid`, `bresp`, `cs_n` | cycle 2: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; cs_n == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 3: expected value of `bvalid` | cycle 3: drive miso=0 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `bvalid`, `bresp` | cycle 6: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `bvalid` | cycle 7: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 17: expected value of `sclk` | cycle 17: no new stimulus this cycle (holds prior-driven values) | sclk == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 18: expected value of `mosi` | cycle 18: no new stimulus this cycle (holds prior-driven values) | mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 27: expected value of `sclk` | cycle 27: no new stimulus this cycle (holds prior-driven values) | sclk == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 38: expected value of `mosi` | cycle 38: no new stimulus this cycle (holds prior-driven values) | mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 58: expected value of `mosi` | cycle 58: no new stimulus this cycle (holds prior-driven values) | mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 78: expected value of `mosi` | cycle 78: no new stimulus this cycle (holds prior-driven values) | mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 98: expected value of `mosi` | cycle 98: no new stimulus this cycle (holds prior-driven values) | mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 118: expected value of `mosi` | cycle 118: no new stimulus this cycle (holds prior-driven values) | mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 138: expected value of `mosi` | cycle 138: no new stimulus this cycle (holds prior-driven values) | mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 158: expected value of `mosi` | cycle 158: no new stimulus this cycle (holds prior-driven values) | mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 167: expected value of `sclk` | cycle 167: no new stimulus this cycle (holds prior-driven values) | sclk == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 169: expected value of `rvalid`, `rdata`, `rresp` | cycle 169: drive arvalid=0 | rvalid == 1; rdata == 2; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 171: expected value of `rvalid`, `rdata`, `rresp` | cycle 171: drive arvalid=0 | rvalid == 1; rdata == 45; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 174: expected value of `bvalid`, `bresp` | cycle 174: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 175: expected value of `bvalid` | cycle 175: drive araddr=0, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-21 | Cycle 176: expected value of `rvalid`, `rdata`, `rresp` | cycle 176: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-22 | Cycle 179: expected value of `bvalid`, `bresp`, `cs_n` | cycle 179: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; cs_n == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-23 | Cycle 180: expected value of `bvalid` | cycle 180: no new stimulus this cycle (holds prior-driven values) | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `aclk` — free-running, `#5` half-period.
- **Reset:** `areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 179 vector cycle(s) in `TbSpec.vectors`, extended to 181 cycle(s) because a `Check` targets a later cycle.
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `aclk` | clock | free-running | n/a |
| `areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `awaddr` | input | yes (varies across the run) | no |
| `awvalid` | input | yes (varies across the run) | no |
| `awready` | output | n/a (output) | no |
| `wdata` | input | yes (varies across the run) | no |
| `wstrb` | input | yes (varies across the run) | no |
| `wvalid` | input | yes (varies across the run) | no |
| `wready` | output | n/a (output) | no |
| `bresp` | output | n/a (output) | yes |
| `bvalid` | output | n/a (output) | yes |
| `bready` | input | yes (varies across the run) | no |
| `araddr` | input | yes (varies across the run) | no |
| `arvalid` | input | yes (varies across the run) | no |
| `arready` | output | n/a (output) | no |
| `rdata` | output | n/a (output) | yes |
| `rresp` | output | n/a (output) | yes |
| `rvalid` | output | n/a (output) | yes |
| `rready` | input | yes (varies across the run) | no |

#### SPI link

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `sclk` | output | n/a (output) | yes |
| `mosi` | output | n/a (output) | yes |
| `miso` | input | yes (varies across the run) | no |
| `cs_n` | output | n/a (output) | yes |

**Gaps:**

- `awready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `cs_released_after_reset` | `$rose(areset) |-> cs_n == 1'd1` | `aclk` | none |
| `sclk_idles_at_cpol_after_reset` | `$rose(areset) |-> sclk == 1'd1` | `aclk` | none |
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- 8-bit transfers only, MSB first: no configurable word length and no LSB-first mode.
- CPOL/CPHA are generate-time options, not register bits — a runtime mode switch would carry both edge behaviours in hardware forever.
- Single chip select. Multiple slaves need external decoding of cs_n.
- No FIFOs: one byte in flight each way, and STATUS.rx_valid must be cleared between exchanges.
- No interrupt output; STATUS must be polled.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
