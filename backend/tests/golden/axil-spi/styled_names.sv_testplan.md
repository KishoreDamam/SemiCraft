# AXI4-Lite SPI Master — Test Plan

An AXI4-Lite SPI master in mode 00. Writing TXDATA performs one full-duplex 8-bit exchange: a byte goes out on mosi while a byte comes in on miso. The AXI frontend is the register-block generator spliced into this module.

_Generated by SemiCraft 0.4.0 — config hash `bea2ab0054b1`. Derived from `axil_spi`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_spi_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- SPI mode: 00 (CPOL=0, CPHA=0)
- Reset clock divisor: 2 aclk cycles per sclk half period (CLKDIV is writable at run time)
- Input synchroniser: 2 stages
- Registers: 5 (32 B decoded, 20 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

**Reset behavior:** The active-low sync reset returns the transfer FSM to idle, puts sclk back to its idle level (0) and releases cs_n, so the master cannot be left driving a slave's bus after a reset. CLKDIV returns to its reset divisor and CTRL.cs_assert clears.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `p_sclk`, `p_csN`, `p_bvalid` | cycle 0: drive p_awaddr=4, p_awvalid=1, p_bready=1, p_wdata=1, p_wstrb=15, p_wvalid=1 | p_sclk == 0; p_csN == 1; p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 2: expected value of `p_bvalid`, `p_bresp`, `p_csN` | cycle 2: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_csN == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 3: expected value of `p_bvalid` | cycle 3: drive p_miso=0 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `p_bvalid`, `p_bresp` | cycle 6: drive p_miso=0 | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `p_bvalid` | cycle 7: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 8: expected value of `p_mosi` | cycle 8: no new stimulus this cycle (holds prior-driven values) | p_mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 9: expected value of `p_sclk` | cycle 9: no new stimulus this cycle (holds prior-driven values) | p_sclk == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 11: expected value of `p_sclk` | cycle 11: no new stimulus this cycle (holds prior-driven values) | p_sclk == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 12: expected value of `p_mosi` | cycle 12: no new stimulus this cycle (holds prior-driven values) | p_mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 16: expected value of `p_mosi` | cycle 16: no new stimulus this cycle (holds prior-driven values) | p_mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 20: expected value of `p_mosi` | cycle 20: no new stimulus this cycle (holds prior-driven values) | p_mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 24: expected value of `p_mosi` | cycle 24: no new stimulus this cycle (holds prior-driven values) | p_mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 28: expected value of `p_mosi` | cycle 28: no new stimulus this cycle (holds prior-driven values) | p_mosi == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 32: expected value of `p_mosi` | cycle 32: no new stimulus this cycle (holds prior-driven values) | p_mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 36: expected value of `p_mosi` | cycle 36: no new stimulus this cycle (holds prior-driven values) | p_mosi == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 39: expected value of `p_sclk` | cycle 39: no new stimulus this cycle (holds prior-driven values) | p_sclk == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 41: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 41: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 2; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 43: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 43: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 45; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 46: expected value of `p_bvalid`, `p_bresp` | cycle 46: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 47: expected value of `p_bvalid` | cycle 47: drive p_araddr=0, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-21 | Cycle 48: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 48: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 0; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-22 | Cycle 51: expected value of `p_bvalid`, `p_bresp`, `p_csN` | cycle 51: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_csN == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-23 | Cycle 52: expected value of `p_bvalid` | cycle 52: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `p_aclk` — free-running, `#5` half-period.
- **Reset:** `p_areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 51 vector cycle(s) in `TbSpec.vectors`, extended to 53 cycle(s) because a `Check` targets a later cycle.
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_aclk` | clock | free-running | n/a |
| `p_areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_awaddr` | input | yes (varies across the run) | no |
| `p_awvalid` | input | yes (varies across the run) | no |
| `p_awready` | output | n/a (output) | no |
| `p_wdata` | input | yes (varies across the run) | no |
| `p_wstrb` | input | yes (varies across the run) | no |
| `p_wvalid` | input | yes (varies across the run) | no |
| `p_wready` | output | n/a (output) | no |
| `p_bresp` | output | n/a (output) | yes |
| `p_bvalid` | output | n/a (output) | yes |
| `p_bready` | input | yes (varies across the run) | no |
| `p_araddr` | input | yes (varies across the run) | no |
| `p_arvalid` | input | yes (varies across the run) | no |
| `p_arready` | output | n/a (output) | no |
| `p_rdata` | output | n/a (output) | yes |
| `p_rresp` | output | n/a (output) | yes |
| `p_rvalid` | output | n/a (output) | yes |
| `p_rready` | input | yes (varies across the run) | no |

#### SPI link

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_sclk` | output | n/a (output) | yes |
| `p_mosi` | output | n/a (output) | yes |
| `p_miso` | input | yes (varies across the run) | no |
| `p_csN` | output | n/a (output) | yes |

**Gaps:**

- `p_awready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `p_wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `p_arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `cs_released_after_reset` | `$rose(areset) |-> cs_n == 1'd1` | `aclk` | none |
| `sclk_idles_at_cpol_after_reset` | `$rose(areset) |-> sclk == 1'd0` | `aclk` | none |
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- 8-bit transfers only, MSB first: no configurable word length and no LSB-first mode.
- CPOL/CPHA are generate-time options, not register bits — a runtime mode switch would carry both edge behaviours in hardware forever.
- Single chip select. Multiple slaves need external decoding of cs_n.
- No FIFOs: one byte in flight each way, and STATUS.rx_valid must be cleared between exchanges.
- No interrupt output; STATUS must be polled.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
