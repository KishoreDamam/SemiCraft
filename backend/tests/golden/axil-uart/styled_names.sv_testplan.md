# AXI4-Lite UART — Test Plan

An AXI4-Lite UART in 8N1 framing — 8 data bits, no parity, one stop bit — with a programmable baud divisor. The AXI frontend is the register-block generator spliced into this module, so the register map the datasheet documents is the one the decode is built from.

_Generated by SemiCraft 0.4.0 — config hash `46430409e63b`. Derived from `axil_uart`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_uart_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Reset baud divisor: 4 clocks per bit (BAUDDIV is writable at run time)
- Input synchroniser: 2 stages
- Framing: 8N1 (fixed)
- Registers: 4 (16 B decoded, 16 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

**Reset behavior:** The active-low sync reset returns both state machines to idle and drives uart_tx high. The receive synchroniser resets high too: coming out of reset with it low would look exactly like a start bit and the receiver would frame on noise. BAUDDIV returns to its reset divisor.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `p_uartTx`, `p_bvalid`, `p_rvalid` | cycle 0: drive p_uartRx=1 | p_uartTx == 1; p_bvalid == 0; p_rvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 2: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 2: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 4; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 5: expected value of `p_bvalid`, `p_bresp` | cycle 5: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `p_bvalid` | cycle 6: drive p_araddr=0, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `p_uartTx`, `p_rvalid`, `p_rdata`, `p_rresp` | cycle 7: drive p_arvalid=0 | p_uartTx == 0; p_rvalid == 1; p_rdata == 1; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 11: expected value of `p_uartTx` | cycle 11: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 15: expected value of `p_uartTx` | cycle 15: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 19: expected value of `p_uartTx` | cycle 19: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 23: expected value of `p_uartTx` | cycle 23: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 27: expected value of `p_uartTx` | cycle 27: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 31: expected value of `p_uartTx` | cycle 31: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 35: expected value of `p_uartTx` | cycle 35: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 39: expected value of `p_uartTx` | cycle 39: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 43: expected value of `p_uartTx` | cycle 43: no new stimulus this cycle (holds prior-driven values) | p_uartTx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 46: expected value of `p_uartTx` | cycle 46: drive p_araddr=0, p_arvalid=1, p_rready=1 | p_uartTx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 47: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 47: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 0; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 95: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 95: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 2; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 97: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 97: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 45; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 100: expected value of `p_bvalid`, `p_bresp` | cycle 100: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 101: expected value of `p_bvalid` | cycle 101: drive p_araddr=0, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-21 | Cycle 102: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 102: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 0; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `p_aclk` — free-running, `#5` half-period.
- **Reset:** `p_areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 103 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_aclk` | clock | free-running | n/a |
| `p_areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_awaddr` | input | yes (varies across the run) | no |
| `p_awvalid` | input | yes (varies across the run) | no |
| `p_awready` | output | n/a (output) | no |
| `p_wdata` | input | yes (varies across the run) | no |
| `p_wstrb` | input | yes (varies across the run) | no |
| `p_wvalid` | input | yes (varies across the run) | no |
| `p_wready` | output | n/a (output) | no |
| `p_bresp` | output | n/a (output) | yes |
| `p_bvalid` | output | n/a (output) | yes |
| `p_bready` | input | yes (varies across the run) | no |
| `p_araddr` | input | yes (varies across the run) | no |
| `p_arvalid` | input | yes (varies across the run) | no |
| `p_arready` | output | n/a (output) | no |
| `p_rdata` | output | n/a (output) | yes |
| `p_rresp` | output | n/a (output) | yes |
| `p_rvalid` | output | n/a (output) | yes |
| `p_rready` | input | yes (varies across the run) | no |

#### Serial

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_uartRx` | input | yes (varies across the run) | no |
| `p_uartTx` | output | n/a (output) | yes |

**Gaps:**

- `p_awready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `p_wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `p_arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `tx_idles_high_after_reset` | `$rose(areset) |-> uart_tx == 1'd1` | `aclk` | none |
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- 8N1 only: no parity, no 5/6/7-bit words, no two-stop-bit mode.
- No FIFOs — one byte deep each way. A second byte arriving before software reads RXDATA sets STATUS.rx_overrun and the older byte is lost.
- Status flags are write-1-to-clear rather than read-to-clear: the register block has no read-side effect, and adding one for a single user was not worth a new access type.
- No hardware flow control (RTS/CTS) and no break detection.
- A framing error sets STATUS.frame_error but the byte is still delivered; the receiver does not resynchronise mid-frame.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
