# AXI4-Lite UART — Test Plan

An AXI4-Lite UART in 8N1 framing — 8 data bits, no parity, one stop bit — with a programmable baud divisor. The AXI frontend is the register-block generator spliced into this module, so the register map the datasheet documents is the one the decode is built from.

_Generated by SemiCraft 0.4.0 — config hash `a2fbff6a6956`. Derived from `axil_uart`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_uart_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Reset baud divisor: 2 clocks per bit (BAUDDIV is writable at run time)
- Input synchroniser: 2 stages
- Framing: 8N1 (fixed)
- Registers: 4 (16 B decoded, 16 B used)
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: Verilog-2001

**Reset behavior:** The active-low sync reset returns both state machines to idle and drives uart_tx high. The receive synchroniser resets high too: coming out of reset with it low would look exactly like a start bit and the receiver would frame on noise. BAUDDIV returns to its reset divisor.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `uart_tx`, `bvalid`, `rvalid` | cycle 0: drive uart_rx=1 | uart_tx == 1; bvalid == 0; rvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 2: expected value of `rvalid`, `rdata`, `rresp` | cycle 2: drive arvalid=0 | rvalid == 1; rdata == 2; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 5: expected value of `bvalid`, `bresp` | cycle 5: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 6: expected value of `bvalid` | cycle 6: drive araddr=0, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `uart_tx`, `rvalid`, `rdata`, `rresp` | cycle 7: drive arvalid=0 | uart_tx == 0; rvalid == 1; rdata == 1; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 9: expected value of `uart_tx` | cycle 9: no new stimulus this cycle (holds prior-driven values) | uart_tx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 11: expected value of `uart_tx` | cycle 11: no new stimulus this cycle (holds prior-driven values) | uart_tx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 13: expected value of `uart_tx` | cycle 13: no new stimulus this cycle (holds prior-driven values) | uart_tx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 15: expected value of `uart_tx` | cycle 15: no new stimulus this cycle (holds prior-driven values) | uart_tx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 17: expected value of `uart_tx` | cycle 17: no new stimulus this cycle (holds prior-driven values) | uart_tx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 19: expected value of `uart_tx` | cycle 19: no new stimulus this cycle (holds prior-driven values) | uart_tx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 21: expected value of `uart_tx` | cycle 21: no new stimulus this cycle (holds prior-driven values) | uart_tx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 23: expected value of `uart_tx` | cycle 23: no new stimulus this cycle (holds prior-driven values) | uart_tx == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 25: expected value of `uart_tx` | cycle 25: no new stimulus this cycle (holds prior-driven values) | uart_tx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 26: expected value of `uart_tx` | cycle 26: drive araddr=0, arvalid=1, rready=1 | uart_tx == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 27: expected value of `rvalid`, `rdata`, `rresp` | cycle 27: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 55: expected value of `rvalid`, `rdata`, `rresp` | cycle 55: drive arvalid=0 | rvalid == 1; rdata == 2; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 57: expected value of `rvalid`, `rdata`, `rresp` | cycle 57: drive arvalid=0 | rvalid == 1; rdata == 45; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 60: expected value of `bvalid`, `bresp` | cycle 60: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 61: expected value of `bvalid` | cycle 61: drive araddr=0, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-21 | Cycle 62: expected value of `rvalid`, `rdata`, `rresp` | cycle 62: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `aclk` — free-running, `#5` half-period.
- **Reset:** `areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 63 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `aclk` | clock | free-running | n/a |
| `areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `awaddr` | input | yes (varies across the run) | no |
| `awvalid` | input | yes (varies across the run) | no |
| `awready` | output | n/a (output) | no |
| `wdata` | input | yes (varies across the run) | no |
| `wstrb` | input | yes (varies across the run) | no |
| `wvalid` | input | yes (varies across the run) | no |
| `wready` | output | n/a (output) | no |
| `bresp` | output | n/a (output) | yes |
| `bvalid` | output | n/a (output) | yes |
| `bready` | input | yes (varies across the run) | no |
| `araddr` | input | yes (varies across the run) | no |
| `arvalid` | input | yes (varies across the run) | no |
| `arready` | output | n/a (output) | no |
| `rdata` | output | n/a (output) | yes |
| `rresp` | output | n/a (output) | yes |
| `rvalid` | output | n/a (output) | yes |
| `rready` | input | yes (varies across the run) | no |

#### Serial

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `uart_rx` | input | yes (varies across the run) | no |
| `uart_tx` | output | n/a (output) | yes |

**Gaps:**

- `awready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `wready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.
- `arready` is an output that no `Check` samples — it is wired to the DUT but never inspected by the self-checking TB.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `tx_idles_high_after_reset` | `$rose(areset) |-> uart_tx == 1'd1` | `aclk` | none |
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- 8N1 only: no parity, no 5/6/7-bit words, no two-stop-bit mode.
- No FIFOs — one byte deep each way. A second byte arriving before software reads RXDATA sets STATUS.rx_overrun and the older byte is lost.
- Status flags are write-1-to-clear rather than read-to-clear: the register block has no read-side effect, and adding one for a single user was not worth a new access type.
- No hardware flow control (RTS/CTS) and no break detection.
- A framing error sets STATUS.frame_error but the byte is still delivered; the receiver does not resynchronise mid-frame.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
