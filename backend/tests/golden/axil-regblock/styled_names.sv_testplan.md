# AXI4-Lite Register Block — Test Plan

An AXI4-Lite target that serves a register map: address decode, byte-strobe writes, per-access-type field behaviour, and a registered read path. The register-map metadata it is built from is the same model the datasheet is rendered from, so the two cannot drift.

_Generated by SemiCraft 0.4.0 — config hash `9e0a82cbd583`. Derived from `axil_regblock`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_regblock_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Data width: 32 bits (4-byte address stride)
- Address width: 5 bits (32 B decoded, 20 B used)
- Registers: 5 (control 1, status 1, irq 1, command 1, scratch)
- Field layout: named sub-fields
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

**Reset behavior:** The active-low sync reset clears both channel handshakes and drops bvalid/rvalid, so the block accepts a transaction on the first cycle after release; every writable field returns to its declared reset value.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `p_bvalid`, `p_rvalid`, `p_awready`, `p_wready`, `p_arready`, `p_ctrl0Enable`, `p_ctrl0Mode`, `p_ctrl0Level`, `p_irq0Flags`, `p_cmd0Go`, `p_cmd0Code`, `p_scratchValue` | cycle 0: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 0; p_rvalid == 0; p_awready == 1; p_wready == 1; p_arready == 1; p_ctrl0Enable == 1; p_ctrl0Mode == 0; p_ctrl0Level == 0; p_irq0Flags == 0; p_cmd0Go == 0; p_cmd0Code == 0; p_scratchValue == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 3: expected value of `p_bvalid`, `p_bresp`, `p_ctrl0Enable`, `p_ctrl0Mode`, `p_ctrl0Level` | cycle 3: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_ctrl0Enable == 0; p_ctrl0Mode == 3; p_ctrl0Level == 15 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 4: expected value of `p_bvalid` | cycle 4: drive p_araddr=0, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 5: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 5: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 246; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 7: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 0; p_rresp == 2 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 10: expected value of `p_bvalid`, `p_bresp`, `p_ctrl0Enable`, `p_ctrl0Mode`, `p_ctrl0Level` | cycle 10: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 2; p_ctrl0Enable == 0; p_ctrl0Mode == 3; p_ctrl0Level == 15 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 11: expected value of `p_bvalid` | cycle 11: drive p_irq0FlagsSet=15 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 12: expected value of `p_irq0Flags` | cycle 12: drive p_irq0FlagsSet=0 | p_irq0Flags == 15 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 15: expected value of `p_bvalid`, `p_bresp`, `p_irq0Flags` | cycle 15: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_irq0Flags == 7 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 16: expected value of `p_bvalid` | cycle 16: drive p_araddr=8, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 17: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 17: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 7; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 20: expected value of `p_bvalid`, `p_bresp`, `p_scratchValue` | cycle 20: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_scratchValue == 2694947491 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 21: expected value of `p_bvalid` | cycle 21: drive p_awaddr=16, p_awvalid=1, p_bready=1, p_wdata=1600019804, p_wstrb=5, p_wvalid=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 23: expected value of `p_bvalid`, `p_bresp`, `p_scratchValue` | cycle 23: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_scratchValue == 2690556508 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 24: expected value of `p_bvalid` | cycle 24: drive p_araddr=16, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-16 | Cycle 25: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 25: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 2690556508; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-17 | Cycle 27: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 27: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 241; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-18 | Cycle 30: expected value of `p_bvalid`, `p_bresp`, `p_cmd0Go`, `p_cmd0Code` | cycle 30: no new stimulus this cycle (holds prior-driven values) | p_bvalid == 1; p_bresp == 0; p_cmd0Go == 1; p_cmd0Code == 7 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-19 | Cycle 31: expected value of `p_bvalid` | cycle 31: drive p_araddr=12, p_arvalid=1, p_rready=1 | p_bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-20 | Cycle 32: expected value of `p_rvalid`, `p_rdata`, `p_rresp` | cycle 32: drive p_arvalid=0 | p_rvalid == 1; p_rdata == 0; p_rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `p_aclk` — free-running, `#5` half-period.
- **Reset:** `p_areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 33 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_aclk` | clock | free-running | n/a |
| `p_areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_awaddr` | input | yes (varies across the run) | no |
| `p_awvalid` | input | yes (varies across the run) | no |
| `p_awready` | output | n/a (output) | yes |
| `p_wdata` | input | yes (varies across the run) | no |
| `p_wstrb` | input | yes (varies across the run) | no |
| `p_wvalid` | input | yes (varies across the run) | no |
| `p_wready` | output | n/a (output) | yes |
| `p_bresp` | output | n/a (output) | yes |
| `p_bvalid` | output | n/a (output) | yes |
| `p_bready` | input | yes (varies across the run) | no |
| `p_araddr` | input | yes (varies across the run) | no |
| `p_arvalid` | input | yes (varies across the run) | no |
| `p_arready` | output | n/a (output) | yes |
| `p_rdata` | output | n/a (output) | yes |
| `p_rresp` | output | n/a (output) | yes |
| `p_rvalid` | output | n/a (output) | yes |
| `p_rready` | input | yes (varies across the run) | no |

#### Hardware face

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_ctrl0Enable` | output | n/a (output) | yes |
| `p_ctrl0Mode` | output | n/a (output) | yes |
| `p_ctrl0Level` | output | n/a (output) | yes |
| `p_status0Busy` | input | yes (varies across the run) | no |
| `p_status0Code` | input | yes (varies across the run) | no |
| `p_irq0Flags` | output | n/a (output) | yes |
| `p_irq0FlagsSet` | input | yes (varies across the run) | no |
| `p_cmd0Go` | output | n/a (output) | yes |
| `p_cmd0Code` | output | n/a (output) | yes |
| `p_scratchValue` | output | n/a (output) | yes |

**Gaps:**

- None found: every port is either driven with varying values or is an output sampled by at least one `Check`.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- No awprot/arprot: the block enforces no protection policy, so declaring inputs it never reads would be dead logic (and would fail the project's -Wall lint gate). An interconnect that drives them simply leaves them unconnected.
- The full byte address is decoded, so an access whose low offset bits are non-zero returns SLVERR rather than aliasing onto the containing word.
- Writing a 1 into a reserved bit returns SLVERR and performs no update — stricter than a block that silently ignores such bits.
- The reset is named areset and renders areset_n; the spec spells it ARESETn. The naming engine owns the active-low suffix, and integration matches on bundle roles rather than spelling.
- One transaction at a time: no write/read overlap, no pipelining.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
