# AXI4-Lite Register Block — Test Plan

An AXI4-Lite target that serves a register map: address decode, byte-strobe writes, per-access-type field behaviour, and a registered read path. The register-map metadata it is built from is the same model the datasheet is rendered from, so the two cannot drift.

_Generated by SemiCraft 0.4.0 — config hash `fc336c14f4f7`. Derived from `axil_regblock`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `axil_regblock_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Data width: 32 bits (4-byte address stride)
- Address width: 4 bits (16 B decoded, 16 B used)
- Registers: 4 (control 1, status 1, irq 1, command 1)
- Field layout: named sub-fields
- Reset: sync, active-low (fixed by AXI4-Lite)
- Output language: SystemVerilog

**Reset behavior:** The active-low sync reset clears both channel handshakes and drops bvalid/rvalid, so the block accepts a transaction on the first cycle after release; every writable field returns to its declared reset value.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `bvalid`, `rvalid`, `awready`, `wready`, `arready`, `ctrl0_enable`, `ctrl0_mode`, `ctrl0_level`, `irq0_flags`, `cmd0_go`, `cmd0_code` | cycle 0: no new stimulus this cycle (holds prior-driven values) | bvalid == 0; rvalid == 0; awready == 1; wready == 1; arready == 1; ctrl0_enable == 1; ctrl0_mode == 0; ctrl0_level == 0; irq0_flags == 0; cmd0_go == 0; cmd0_code == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 3: expected value of `bvalid`, `bresp`, `ctrl0_enable`, `ctrl0_mode`, `ctrl0_level` | cycle 3: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; ctrl0_enable == 0; ctrl0_mode == 3; ctrl0_level == 15 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 4: expected value of `bvalid` | cycle 4: drive araddr=0, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 5: expected value of `rvalid`, `rdata`, `rresp` | cycle 5: drive arvalid=0 | rvalid == 1; rdata == 246; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 7: expected value of `rvalid`, `rdata`, `rresp` | cycle 7: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 2 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 10: expected value of `bvalid`, `bresp`, `ctrl0_enable`, `ctrl0_mode`, `ctrl0_level` | cycle 10: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 2; ctrl0_enable == 0; ctrl0_mode == 3; ctrl0_level == 15 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 11: expected value of `bvalid` | cycle 11: drive irq0_flags_set=15 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 12: expected value of `irq0_flags` | cycle 12: drive irq0_flags_set=0 | irq0_flags == 15 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 15: expected value of `bvalid`, `bresp`, `irq0_flags` | cycle 15: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; irq0_flags == 7 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 16: expected value of `bvalid` | cycle 16: drive araddr=8, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-11 | Cycle 17: expected value of `rvalid`, `rdata`, `rresp` | cycle 17: drive arvalid=0 | rvalid == 1; rdata == 7; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-12 | Cycle 19: expected value of `rvalid`, `rdata`, `rresp` | cycle 19: drive arvalid=0 | rvalid == 1; rdata == 241; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-13 | Cycle 22: expected value of `bvalid`, `bresp`, `cmd0_go`, `cmd0_code` | cycle 22: no new stimulus this cycle (holds prior-driven values) | bvalid == 1; bresp == 0; cmd0_go == 1; cmd0_code == 7 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-14 | Cycle 23: expected value of `bvalid` | cycle 23: drive araddr=12, arvalid=1, rready=1 | bvalid == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-15 | Cycle 24: expected value of `rvalid`, `rdata`, `rresp` | cycle 24: drive arvalid=0 | rvalid == 1; rdata == 0; rresp == 0 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `aclk` — free-running, `#5` half-period.
- **Reset:** `areset_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 25 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `aclk` | clock | free-running | n/a |
| `areset_n` | reset | reset sequence only | n/a |

#### AXI4-Lite slave

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `awaddr` | input | yes (varies across the run) | no |
| `awvalid` | input | yes (varies across the run) | no |
| `awready` | output | n/a (output) | yes |
| `wdata` | input | yes (varies across the run) | no |
| `wstrb` | input | yes (varies across the run) | no |
| `wvalid` | input | yes (varies across the run) | no |
| `wready` | output | n/a (output) | yes |
| `bresp` | output | n/a (output) | yes |
| `bvalid` | output | n/a (output) | yes |
| `bready` | input | yes (varies across the run) | no |
| `araddr` | input | yes (varies across the run) | no |
| `arvalid` | input | yes (varies across the run) | no |
| `arready` | output | n/a (output) | yes |
| `rdata` | output | n/a (output) | yes |
| `rresp` | output | n/a (output) | yes |
| `rvalid` | output | n/a (output) | yes |
| `rready` | input | yes (varies across the run) | no |

#### Hardware face

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `ctrl0_enable` | output | n/a (output) | yes |
| `ctrl0_mode` | output | n/a (output) | yes |
| `ctrl0_level` | output | n/a (output) | yes |
| `status0_busy` | input | yes (varies across the run) | no |
| `status0_code` | input | yes (varies across the run) | no |
| `irq0_flags` | output | n/a (output) | yes |
| `irq0_flags_set` | input | yes (varies across the run) | no |
| `cmd0_go` | output | n/a (output) | yes |
| `cmd0_code` | output | n/a (output) | yes |

**Gaps:**

- None found: every port is either driven with varying values or is an output sampled by at least one `Check`.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `bvalid_idle_after_reset` | `$rose(areset) |-> bvalid == 1'd0` | `aclk` | none |
| `rvalid_idle_after_reset` | `$rose(areset) |-> rvalid == 1'd0` | `aclk` | none |

## Not Covered / Limitations

- No awprot/arprot: the block enforces no protection policy, so declaring inputs it never reads would be dead logic (and would fail the project's -Wall lint gate). An interconnect that drives them simply leaves them unconnected.
- The full byte address is decoded, so an access whose low offset bits are non-zero returns SLVERR rather than aliasing onto the containing word.
- Writing a 1 into a reserved bit returns SLVERR and performs no update — stricter than a block that silently ignores such bits.
- The reset is named areset and renders areset_n; the spec spells it ARESETn. The naming engine owns the active-low suffix, and integration matches on bundle roles rather than spelling.
- One transaction at a time: no write/read overlap, no pipelining.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
