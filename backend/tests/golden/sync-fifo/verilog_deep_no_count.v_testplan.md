# Synchronous FIFO — Test Plan

A 256-entry, 8-bit synchronous FIFO. Read and write pointers carry an extra wrap bit, so full and empty are distinguishable and the occupancy count is exact without any separate counter or saturating logic.

_Generated by SemiCraft 0.3.0 — config hash `ac4b61ce8c0b`. Derived from `sync_fifo`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `sync_fifo_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Width: 8 bits
- Depth: 256 entries (power of two; 8-bit index + wrap bit)
- Count output: no
- Reset: sync, active-low
- Output language: Verilog-2001

**Reset behavior:** The sync active-low reset returns both pointers to zero, so the FIFO reads empty and not full immediately. The storage array is deliberately not cleared — stale entries are unreachable while empty, and clearing a deep memory on reset costs logic for no observable difference.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `empty`, `full`, `rd_data` | cycle 0: no new stimulus this cycle (holds prior-driven values) | empty == 1; full == 0; rd_data == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 5: expected value of `empty`, `full` | cycle 5: drive rd_en=1, wr_en=0 | empty == 0; full == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 6: expected value of `rd_data`, `empty`, `full` | cycle 6: no new stimulus this cycle (holds prior-driven values) | rd_data == 1; empty == 0; full == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 7: expected value of `rd_data`, `empty`, `full` | cycle 7: no new stimulus this cycle (holds prior-driven values) | rd_data == 2; empty == 0; full == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 8: expected value of `rd_data`, `empty`, `full` | cycle 8: no new stimulus this cycle (holds prior-driven values) | rd_data == 3; empty == 0; full == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 9: expected value of `rd_data`, `empty`, `full` | cycle 9: no new stimulus this cycle (holds prior-driven values) | rd_data == 4; empty == 1; full == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 266: expected value of `empty`, `full` | cycle 266: no new stimulus this cycle (holds prior-driven values) | empty == 0; full == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 268: expected value of `empty`, `full` | cycle 268: drive wr_en=0 | empty == 0; full == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 525: expected value of `empty`, `full`, `rd_data` | cycle 525: no new stimulus this cycle (holds prior-driven values) | empty == 1; full == 0; rd_data == 255 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 527: expected value of `empty`, `full`, `rd_data` | cycle 527: drive rd_en=0 | empty == 1; full == 0; rd_data == 255 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `clk` — free-running, `#5` half-period.
- **Reset:** `rst_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 529 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `clk` | clock | free-running | n/a |
| `rst_n` | reset | reset sequence only | n/a |

#### Write port

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `wr_en` | input | yes (varies across the run) | no |
| `wr_data` | input | yes (varies across the run) | no |
| `full` | output | n/a (output) | yes |

#### Read port

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `rd_en` | input | yes (varies across the run) | no |
| `rd_data` | output | n/a (output) | yes |
| `empty` | output | n/a (output) | yes |

**Gaps:**

- None found: every port is either driven with varying values or is an output sampled by at least one `Check`.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `rd_data_reset_value` | `$rose(rst) |-> rd_data == 8'd0` | `clk` | none |
| `empty_after_reset` | `$rose(rst) |-> empty == 1'd1` | `clk` | none |
| `not_full_after_reset` | `$rose(rst) |-> full == 1'd0` | `clk` | none |

## Not Covered / Limitations

- Reads are registered (plain FIFO), so rd_data is valid the cycle after rd_en. There is no first-word-fall-through mode: FWFT is a different read contract, not a flag, and offering both behind one boolean produces a datasheet that describes neither exactly.
- Overflow and underflow are ignored silently — a write while full and a read while empty leave the FIFO untouched, with no error output to detect that it happened.
- Single clock domain only. Crossing clocks needs the gray-pointer asynchronous FIFO, which is a separate IP.
- No almost-full / almost-empty thresholds.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
