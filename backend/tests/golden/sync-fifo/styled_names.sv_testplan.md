# Synchronous FIFO — Test Plan

A 8-entry, 8-bit synchronous FIFO. Read and write pointers carry an extra wrap bit, so full and empty are distinguishable and the occupancy count is exact without any separate counter or saturating logic.

_Generated by SemiCraft 0.4.0 — config hash `aa4576c29109`. Derived from `sync_fifo`'s `ExplanationDoc`, `port_groups()`, and `tb_spec()`; the Test Plan table mirrors exactly what `sync_fifo_tb.sv` drives and checks._

## Features Under Test

Configuration exercised by this test plan:

- Width: 8 bits
- Depth: 8 entries (power of two; 3-bit index + wrap bit)
- Count output: yes
- Reset: sync, active-low
- Output language: SystemVerilog

**Reset behavior:** The sync active-low reset returns both pointers to zero, so the FIFO reads empty and not full immediately. The storage array is deliberately not cleared — stale entries are unreachable while empty, and clearing a deep memory on reset costs logic for no observable difference.

## Test Plan

One row per directed cycle that carries a `Check` (cycle indices are 0-based, counted from the first cycle after reset release — matching `generate_tb`'s own `checks_by_cycle` grouping). Every row is a directed, self-checking assertion emitted as an `if (sig !== expected) $fatal(...)` in the generated TB.

| ID | Feature / Intent | Stimulus | Expected Result | Status |
|---|---|---|---|---|
| TP-01 | Cycle 0: expected value of `p_empty`, `p_full`, `p_count`, `p_rdData` | cycle 0: no new stimulus this cycle (holds prior-driven values) | p_empty == 1; p_full == 0; p_count == 0; p_rdData == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-02 | Cycle 5: expected value of `p_empty`, `p_full`, `p_count` | cycle 5: drive p_rdEn=1, p_wrEn=0 | p_empty == 0; p_full == 0; p_count == 4 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-03 | Cycle 6: expected value of `p_rdData`, `p_empty`, `p_full`, `p_count` | cycle 6: no new stimulus this cycle (holds prior-driven values) | p_rdData == 1; p_empty == 0; p_full == 0; p_count == 3 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-04 | Cycle 7: expected value of `p_rdData`, `p_empty`, `p_full`, `p_count` | cycle 7: no new stimulus this cycle (holds prior-driven values) | p_rdData == 2; p_empty == 0; p_full == 0; p_count == 2 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-05 | Cycle 8: expected value of `p_rdData`, `p_empty`, `p_full`, `p_count` | cycle 8: no new stimulus this cycle (holds prior-driven values) | p_rdData == 3; p_empty == 0; p_full == 0; p_count == 1 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-06 | Cycle 9: expected value of `p_rdData`, `p_empty`, `p_full`, `p_count` | cycle 9: no new stimulus this cycle (holds prior-driven values) | p_rdData == 4; p_empty == 1; p_full == 0; p_count == 0 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-07 | Cycle 18: expected value of `p_empty`, `p_full`, `p_count` | cycle 18: no new stimulus this cycle (holds prior-driven values) | p_empty == 0; p_full == 1; p_count == 8 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-08 | Cycle 20: expected value of `p_empty`, `p_full`, `p_count` | cycle 20: drive p_wrEn=0 | p_empty == 0; p_full == 1; p_count == 8 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-09 | Cycle 29: expected value of `p_empty`, `p_full`, `p_count`, `p_rdData` | cycle 29: no new stimulus this cycle (holds prior-driven values) | p_empty == 1; p_full == 0; p_count == 0; p_rdData == 255 | Directed check in generated TB (`$fatal` on mismatch) |
| TP-10 | Cycle 31: expected value of `p_empty`, `p_full`, `p_count`, `p_rdData` | cycle 31: drive p_rdEn=0 | p_empty == 1; p_full == 0; p_count == 0; p_rdData == 255 | Directed check in generated TB (`$fatal` on mismatch) |

## Coverage / Stimulus Summary

- **Clock:** `p_clk` — free-running, `#5` half-period.
- **Reset:** `p_rst_n` — held asserted for 2 cycle(s) before the first directed vector.
- **Directed run length:** 33 vector cycle(s).
- **Port value constraints:** none declared — every driven value is only masked to its port's bit width, not clamped to a sub-range.

### Ports exercised

#### Clocking

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_clk` | clock | free-running | n/a |
| `p_rst_n` | reset | reset sequence only | n/a |

#### Write port

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_wrEn` | input | yes (varies across the run) | no |
| `p_wrData` | input | yes (varies across the run) | no |
| `p_full` | output | n/a (output) | yes |

#### Read port

| Port | Direction | Driven | Checked |
|---|---|---|---|
| `p_rdEn` | input | yes (varies across the run) | no |
| `p_rdData` | output | n/a (output) | yes |
| `p_empty` | output | n/a (output) | yes |
| `p_count` | output | n/a (output) | yes |

**Gaps:**

- None found: every port is either driven with varying values or is an output sampled by at least one `Check`.

## Assertions (SVA)

The properties below are the *actual* output of `assertions.generate_assertions(tb_spec.assertion_spec)` — the same call `generate_tb` makes — so this table cannot drift from the concurrent-assertion block the TB emits.

| Name | Property | Clock | Guard |
|---|---|---|---|
| `rd_data_reset_value` | `$rose(rst) |-> rd_data == 8'd0` | `clk` | none |
| `empty_after_reset` | `$rose(rst) |-> empty == 1'd1` | `clk` | none |
| `not_full_after_reset` | `$rose(rst) |-> full == 1'd0` | `clk` | none |
| `count_zero_after_reset` | `$rose(rst) |-> count == 4'd0` | `clk` | none |

## Not Covered / Limitations

- Reads are registered (plain FIFO), so rd_data is valid the cycle after rd_en. There is no first-word-fall-through mode: FWFT is a different read contract, not a flag, and offering both behind one boolean produces a datasheet that describes neither exactly.
- Overflow and underflow are ignored silently — a write while full and a read while empty leave the FIFO untouched, with no error output to detect that it happened.
- Single clock domain only. Crossing clocks needs the gray-pointer asynchronous FIFO, which is a separate IP.
- No almost-full / almost-empty thresholds.
- Directed, not randomized: the smoke TB only runs the fixed vectors in `TbSpec.vectors`; no constrained-random or exhaustive stimulus is attempted.
- Default parameterization only: the generated TB instantiates the DUT with no parameter overrides, so only the exact configuration used to generate this file is exercised. A different parameterization of the same RTL is not covered by this TB.
- Single clock/reset domain only: this TB drives one free-running clock and (if present) one reset; it performs no clock-domain-crossing verification.
