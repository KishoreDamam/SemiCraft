# LFSR

A 24-bit Fibonacci LFSR: a pseudo-random binary sequence generator with parallel output, period 2^24-1.

_Generated by SemiCraft 0.1.0 — config hash `81c4bc40fac4`._

## Ports

### Clocking

Clock, reset, and (optional) shift enable for the LFSR register.

| Port | Direction | Description |
| --- | --- | --- |
| `clk` | input | Clock; the register shifts on the rising edge. |
| `rst` | input | Async reset, active-high reset input. |
| `en` | input | Shift enable; when low the register holds its current value. |

### Data

Full LFSR register state.

| Port | Direction | Description |
| --- | --- | --- |
| `q` | output | 24-bit LFSR register state. |

## Configuration

- Width: 24 bits (param WIDTH)
- Taps: (24, 23, 22, 17) (1-indexed from LSB; standard maximal-length polynomial)
- Seed (init_value): 11259361 (param INIT)
- Enable input: yes
- Output style: parallel
- Reset: async, active-high
- Output language: SystemVerilog

## Assumptions

- A single free-running clock drives the register; all I/O is synchronous to it.
- The seed (init_value) is loaded once, at reset; changing it requires regenerating the module (there is no runtime seed-load input).

## Limitations

- This is a pseudo-random sequence generator, not a cryptographically secure random number generator -- do not use it for security-sensitive randomness (key/nonce generation, etc.).
- Maximal-length Fibonacci LFSR: starting from any nonzero seed, it cycles through all 16777215 nonzero 24-bit states before repeating (period 2^24-1 = 16777215).
- All-zero is an illegal lockup state for this Fibonacci LFSR (the XOR feedback function produces 0 forever once the register reaches it); init_value is validated to be non-zero and reset loads init_value (never zero), so a correctly generated instance never starts in, and (being maximal-length) never reaches, the lockup state.
